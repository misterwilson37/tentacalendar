# HANDOFF.md — Tentacalendar
**Document version: 0.51.0** | Last updated: 2026-07-15 | **Billye** (Claude Opus, 5th instance) picked up 2026-07-15: shipped D97 — the victories/put-offs cards Rambo scoped but never built, and the TIDAL GRID as layout #2 under Week. Names taken: Inky, Otto, Rambo, Billye. | ⚠️ **MODEL TRANSITION:** Otto (Claude Fable 5) signs off with this version — Fable support ends 2026-07-12; the next instance will likely be **Claude Opus**. Nothing about the rituals changes: read §2 and §6, run the ship-checks, name yourself (taken: Inky, Otto), and keep Jake's and Katie's trust the way it was earned — one asserted replacement at a time. | Last session (Otto, rounds 1–3): stale-css diagnosis + D64 rows → Phase 2 shipped (D65) → Ends-box flex-min-content fix + D66 day texture/density → D68 MONTH-GRID layout ("this view is...nuts" — the Gantt now has a wall-calendar sibling and a toggle; grid is default), window-height timeline sizing, ＋ New project modal (form reparenting). Ends box CONFIRMED FIXED by Jake. | **Rambo** (Claude Opus) picked up 2026-07-14 — read cold, named (photographer octopus, for the "views" session), shipped D85 clear-the-deck priority as the first build. Carryover (§4 ③) remains the open quest; recurring tasks + creation-time follow-up chains queued in §5c.

> **Purpose:** Project continuity document, updated at the END of every session, no exceptions (same ritual status as version bumps). Any Claude — Fable, Opus, or otherwise — reading this cold should be able to continue mid-feature. New Claude: read this entire file before writing code.

---

## 1. What Tentacalendar Is

Accountability + planning calendar for Katie's business (and household): a to-do list that *refuses to let things silently disappear*. Named for her octopus brain. tentacalendar.misterwilson.org (CNAME + repo exist; first deploy pending — Jake follows SETUP.md).

Faces: (1) 60"+ 4K big screen — annual calendar left, week top-right, agenda + to-do queue bottom-right (a projects pipeline column now also exists in the phase-1 layout); (2) same responsive app on phones, queue-first. Katie is now an active stakeholder with her own requirements (see D28–D32) — treat her requests as first-class.

Interim displays: home-theater 60" 4K TV and possibly a 1080p projector (see D27).

## 2. Locked Architectural Decisions

| # | Decision | Rationale |
|---|----------|-----------|
| D1 | Split source of truth: GCal owns EVENTS (Home + Business calendars, native editing, native recurrence). Firestore owns TASKS, PROJECTS (incl. pipeline stages), TIERS, overlays. | |
| D2 | Mirror stable truth, not churn: outbound mirror = project bars + tasks at ORIGINAL due time only. | |
| D3 | Escalation = render-time computation. `{dueAt, completedAt, escalation:{every,unit}}`; displayed slot = next step ≥ now, "reschedule theater" struck-through original. Default 1h, per-task dropdown. | |
| D4 | Relative follow-ups materialize on parent completion (`parentTaskId`+`offsetDays`); shown under "Waiting on…". Un-completing parent does NOT rewind children. | |
| D5 | Entity types: Projects (now with embedded stage pipelines), Events (GCal anchors), Tasks. | |
| D6 | ~~Priority = tier ordering~~ **SUPERSEDED BY D43** (v0.6.0). | |
| D7 | Anchors pin above all tiers within lead window (default 30 min); never escalate. `requireAcknowledge` reserved v1.1. | |
| D8 | No collision-aware auto-scheduling v1. | |
| D9 | GitHub Pages + Firebase. NOT PLEXBEAST. | |
| D10 | Firebase Auth, Google provider, two-email allowlist (config.js + firestore.rules MUST match). | |
| D11 | Year view = 12-month grid; REVISED by D31 (quarter-aligned + 3 anchor modes). Gantt toggle deferred. | |
| D12 | Workspace in schema; v1 builds only `primary`. | |
| D13 | Name: Tentacalendar. | 🐙 |
| D14 | Midnight carryover fn: incomplete tasks in `midnightCarryover` tiers → next-morning GCal events, "❗" prefix, tomato (colorId 11), landing at `carryoverWriteHour` (default 9, in settings). Phase 3. | |
| D15 | Inbound events: scheduled poll → eventsCache; default 60 min, settable (5–1440) w/ cost readout; manual "refresh now" button in phase 3; poll MUST diff-write. | |
| D16 | Event tiers via per-calendar defaults + sparse eventMeta overlay. | |
| D17 | Outbound mirror trigger: Firestore onWrite. | |
| D18 | Month zoom v1; custom zoom levels v2. | |
| D19 | Offline/PWA deferred indefinitely. | |
| D20 | Recurring EVENTS via GCal (free); recurring TASKS v2; no improvised recurrence engines. | |
| D21 | Burn-in: near-black theme + 2px sinusoidal drift (shipped) + scheduled sleep (settings shipped; dim mode ships w/ kiosk phase). | |
| D22 | 1-min tick, midnight rollover, day nav arrows + back-to-today (shipped). | |
| D23 | Standalone Firebase project under jacob.v.wilson@gmail.com; billing movable per-project later. | |
| D24 | Phase 1 = FREE SPARK PLAN, no card. Blaze only at phase 3; realistic bill ≈ $0.00. | |
| D25 | Completed mirrored tasks get "✓" prefix on GCal, kept for the record (phase 3). | |
| D26 | Modular files: config.js (only hand-edited file), store.js (Firebase), queue.js (pure logic), app.js (UI), celebrate.js (dopamine), index.html, tentacalendar.css, firestore.rules. | |
| D27 | 1080p/projector compact mode for year view: labels hide below width threshold; legend + hover tooltips ship with year view day one. | |
| D28 | **(NEW — Katie #1) Project pipelines.** Universal stage TEMPLATE in settings (name, phase before/during/after, ± day offset, reorderable); each NEW project snapshots the template into an independently editable `stages` array. Queue surfaces exactly ONE item per project: the EARLIEST unchecked stage, and only once its activation date arrives (before: start−offset; during: start; after: end+offset). Stages checked early in the project panel are simply skipped when the pipeline reaches them. Optional per-stage hard `dueAt` (📅) makes that stage escalate/float like a task. SHIPPED v0.2.0. Seed template = Katie's 13 stages: Engagement letter (before −14d), Data request, Excel setup, Word setup, Graphic setup, Loss data processing, Non-loss data processing, Data input, Selections, Proofing, Peer review, Publication, Client follow-up (after +14d). | Katie's core workflow. |
| D29 | **(NEW — Katie #2) Celebration engine** (celebrate.js): level 1 task = randomized confetti pop at the checkbox; level 2 stage = double burst + streamers; level 3 project complete = fireworks + Katie's wave sweeping the screen (CSS `celebration-wave`). Variants randomized against habituation. SHIPPED v0.2.0. | Dopamine is a feature, not a gimmick. |
| D30 | **(NEW — Katie #3, split) Gradient feedback:** (a) Year-view project bars render as pale "ghost" of project color with saturated fill left-to-right by pipeline % (`projectProgress()` already in queue.js) — clearer than brightness-only; ships with year view. Progress bar version SHIPPED in project panel v0.2.0. (b) Queue staleness glow: overdue rows grow an intensifying red glow with days ignored (whisper day 1 → searchlight day 4+). SHIPPED v0.2.0. Tier chip colors stay stable (D6 predictability). | Katie's eye-drawing request, made legible. |
| D31 | **(NEW — Katie #4) Year view spec (phase 2):** quarter-aligned 4 rows × 3 months; THREE anchor modes — Calendar (Jan–Dec), Quarter-first (current fiscal quarter top-left, rolling 12), Month-first (current month top-left, rolling 12). Rolling modes may reallocate panel proportions. | Her clients work on quarters. |
| D32 | **(NEW — Katie #5) Year view interactivity (phase 2):** project bars support drag-to-move and edge-drag to extend/shorten (pointer events over the grid). Whole-year planning that filters down to hourly tasks is the point. | |
| D33 | **(NEW v0.3.0) Calendar IDs live ON anchor tiers** (`gcalCalendarId` field, shown in tier editor when kind=Calendar), replacing the separate homeCalendarId/businessCalendarId config fields (REMOVED from settings/config and UI). Phase 3 pollCalendars iterates anchor tiers with non-empty gcalCalendarId. | Jake's setup feedback: the tier IS the calendar mapping; separate section created an ordering/linkage gap. |
| D34 | **(NEW v0.3.0) "During" stage offset = N days AFTER project start** (0 = day one), still gated on predecessors. Previously the offset was silently ignored for during-phase. | Jake: "what does during do if I set it for 7?" — now it does the obvious thing. |
| D35 | **(NEW v0.3.0) CSS must contain `[hidden] { display: none !important; }`** — the hidden attribute is UA-stylesheet priority and ANY author display rule silently overrides it. This caused the first-deploy failure (settings modal permanently rendered over the sign-in screen; all Firestore writes fired unauthenticated → permission errors). NEVER remove this rule; when an element with hidden won't hide, check for a display rule on it. | First-deploy post-mortem. |
| D36 | **(REVISED v0.4.0) Recommended browser: Microsoft Edge** (Mac+PC, Chromium, no OAuth-eating shields) — Chrome is county-managed on Jake's machines. Safari confirmed working. Firefox still banned (popup-loop history). Personal Google accounts only. | |
| D37 | **(NEW v0.4.0) NEVER apply CSS transform to <body> or any ancestor of position:fixed elements** — a transformed ancestor becomes the containing block for fixed descendants (this made the settings modal center against full PAGE height, appearing far below the fold once Katie had 6 projects). The pixel drift lives on #drift-wrap; the settings modal and the celebrate.js canvas MUST stay outside it. Second CSS-platform landmine after D35. | Found by Jake changing a tier color. |
| D38 | **(NEW v0.4.0 — Katie) Everything is editable.** ✎ on queue tasks loads the task form in edit mode (title/tier/due/escalation, Save/Cancel); ✎ on project cards loads the project form in edit mode (name/color/tier/dates); ✎ on waiting follow-ups edits title/offset via prompts. Editing project dates reflows stage activations automatically (computed, D3-style). Stage hard dues already editable via 📅 (blank clears). | "Us humans make mistakes — we need to be able to make changes to everything." |
| D39 | **(NEW v0.4.0 — Katie) Queue progress wash:** stage rows' backgrounds fill left-to-right with a translucent tint (16%) of the project color by pipeline completion %, so today's list shows step AND depth per project at a glance. | Her at-a-glance request, queue edition of D30a. |
| D40 | **(NEW v0.4.0 — Katie) Color conflict assistant:** project form pre-suggests the pool color maximally distant from existing project colors (never stomps a human-picked value); live warning when chosen color is within RGB distance 25 ("same as") or 60 ("very close to") of another project, naming that project and its date range, plus a suggestion. | Several of her 6 projects currently share colors. |
| D42 | **(v0.5.0) Per-project stage surgery:** "✎⋮" on each project card opens an editor to rename/reorder/add/remove stages on a LIVE project; completedAt + dueAt survive via original-index carry. Template stays a template (new projects only). | Katie: "Not all projects are standard." |
| D43 | **(v0.6.0) PRIORITY ENGINE v2 — the queue sort is now:** 1) EXPIRED first (missed = top, oldest deadline first); 2) CHRONOLOGICAL by honest due/deadline — tier does NOT matter here (a low-tier 9 AM beats a high-tier noon); 3) tie → remaining-pipeline-fraction × WORKLOAD, higher first (tasks/events score 0); 4) tie → tier rank; 5) tie → alphabetical. Tasks sort on ORIGINAL due (escalation theater is display-only). Computed date-only deadlines are "by 5 PM" that day so 9 AM tasks outrank them and midnight doesn't turn things red. | Katie + Jake's workflow analysis. She sees WHAT; this gives ORDER. |
| D44 | **(v0.6.0) Workload on projects:** 1 light / 2 standard / 3 heavy (default 2). Multiplies remaining pipeline in D43 level 3. Also: stage timing is now direction(before/after) × anchor(start/end) × offsetDays, replacing phase before/during/after — supports "peer review 2wd before END", "loss processing Nwd after START". Legacy phase data migrates lazily via normalizeStage() everywhere it's read; writes upgrade on next save. Each stage's computed date (scheduledAt) IS its deadline; a manual dueAt overrides it. Project queue item shows/sorts on nextDeadline = earliest effective date among incomplete stages ("next: Peer review — Wed, Jul 29"), replacing the useless "active since". | |
| D45 | **(v0.6.0) WEEKDAY math everywhere:** stage offsets count weekdays only (addWeekdays skips Sat/Sun). Project start/end dates are intercepted if they land on a weekend: modal offers Yes / No—Friday X / No—Monday Y / Go back (checked start then end). Stage-level dates need no modal — they're computed and weekend-proof by construction. | Katie works weekdays. |
| D46 | **(v0.6.0) Overdue decision modal:** items ≥2 days past deadline (visible tiers only) trigger ONE grouped modal, at most once per calendar day per device (localStorage key), each row offering ✓ done and ↷ reschedule-to-next-weekday-9AM; dismissible. Hidden tiers are excluded, so unhiding never avalanches (that day's shot is already spent). | The final escalation step. |
| D47 | **(v0.6.0) Tier filter chips** above the queue: tap to hide/show a tier on THIS DEVICE (localStorage 'tc-hidden-tiers'). Hiding a rank ≤4 tier requires confirm(). Hidden tiers vanish from queue, pinned, done-today, and the decision modal; project cards remain visible. | "Ignore the taiko stuff rather than ignore the app entirely." |
| D48 | **(v0.6.0) Pipeline windows gate visibility:** a project appears in the queue only from its EARLIEST effective stage date (e.g., engagement letter at start−14wd) through its last; before that window it's invisible (future projects don't flood today); past it, it lives on "today" as expired until dealt with. | Jake's finally⁴: a December project must not appear in July. |
| D50 | **(v0.6.2) UNDATED STAGES ARE FIRST-CLASS — most pipeline steps are weight, not deadlines.** Stage timing = No date | Before | After (direction:"none" in schema). Undated stages: no scheduledAt, never overdue, never a queue deadline — they matter through remaining-pipeline × workload (they are "the number of them before the next step"). Project deadline = next DATED commitment (hard due or dated offset); fallback = project end at 5 PM. **Legacy phase "during" migrates to UNDATED** — this instantly un-overdued Katie's data with zero DB writes (the previous mapping to after-start+0 stamped every stage with the project start date → "69 days overdue" apocalypse). Anchor/offset controls hide when No date is selected. CAVEAT: any stage saved through the 0.6.0/0.6.1 editors was written as after/start/0 (dated at start) — flip those to No date via ✎⋮ if they misbehave; probably none exist. | Jake: "it's about the number of them before the next step, not each one individually." My model was wrong; his is canonical. |
| D49 | **(v0.6.1) ES module imports MUST carry version queries.** `?v=` on `<script>`/`<link>` tags does NOT cache-bust `import` statements inside modules — a stale cached config.js (predating CONFIG_VERSION) made the whole module graph fail with `SyntaxError: Importing binding name not found` and a dead sign-in button. All internal imports now use `./file.js?v=x.y.z`, kept IDENTICAL across importers (differing queries = duplicate module instances). **Bump ritual now includes: when bumping a module, update its ?v= string in every importer** (config.js ← app.js + store.js; store/queue/celebrate ← app.js; app.js ← index.html). Third platform landmine (D35 hidden/display, D37 transform/fixed, D49 module cache). | |
| D41 | **(v0.4.0) Per-file versioning:** only files that actually changed get bumps; versions drift apart by design. Every JS module self-declares (APP_VERSION in app.js — moved OUT of config.js; STORE_VERSION; QUEUE_VERSION; CELEBRATE_VERSION), CSS declares --tc-version, index.html declares body[data-html-version]. Header badge = app.js version; hovering it and the settings-modal footer show the full per-file report. Fix-dominant releases bump z; feature releases bump y. | Jake's correction of my blanket-bump habit. |
| D51 | **(v0.7.0) Deadline hour = 4 PM, configurable.** Computed (date-only) stage deadlines count as "due by config.deadlineHour:00" (default 16). Settable in ⚙️ Timing with an explanation; queue.js holds it in a module setter (setDeadlineHour) fed by the config subscription. Manual ⏰ dues keep their own times. | Jake's answer to closeout Q-A: "4 PM. Configurable." |
| D52 | **(v0.7.0) Decision threshold configurable + settings tabs.** config.decisionThresholdDays (default 2) drives the overdue check-in; settings modal split into Tiers / Pipeline / Timing tabs (it outgrew one scroll). | Jake's answer to Q-B. |
| D53 | **(v0.7.0) Un-complete rewind modal.** Un-checking a parent whose completion materialized follow-ups asks: (1) Oops — keep it done; (2) Un-complete & pull follow-ups back to "Waiting on…" (store.rewindFollowUps nulls dueAt on incomplete children); (3) Un-complete but follow-ups keep their dates (they're already booked/underway). Children that HAVE a dueAt were materialized — follow-ups are born dueAt:null. Completed children are never touched. | Jake's answer to Q-C, third option included. |
| D54 | **(v0.7.0) Monthly escalation clamp accepted** (Jan 31 → Feb 28). No code change; behavior is canon. | Jake's answer to Q-D: "forget it." |
| D55 | **(v0.7.0) Tier color conflict assistant** — same-as/very-close warning (RGB distance 25/60) naming the colliding pair + a maximally-distant TIER_PALETTE suggestion, live in ⚙️ Tiers under the editor (#tier-color-hint). New-tier rows already auto-picked fresh colors. | Jake's answer to Q-E. |
| D56 | **(v0.7.0) Collapsible project cards.** Cards collapse to [chevron][name][pinned 🔁✎✎⋮✕ buttons] + dates line + progress bar; tapping the header toggles the stage list (localStorage 'tc-expanded-projects', default collapsed). The header row NEVER wraps — names wrap inside their own box (Katie's cards were "way too much scrolling", and buttons were falling to a second row). Finished projects fold into a "▸ Finished ✓ (N)" section at the bottom ('tc-show-finished'); before this they sat dimmed in place forever. Unfinished projects stay visible whatever their dates — a stalled past project is exactly what accountability is for. | |
| D57 | **(v0.7.0) Decision modal v2 — rows are LIVE.** renderDecision() re-derives rows from current state on every render (keyed once at open: proj:{id} / task:{id}), so actions visibly resolve, a project row advances to its next stage, and the modal auto-closes when empty. ✓ completes the ACTIVE stage (it.stageIndex — the 0.6.x code completed deadlineStageIndex, which is why Jake's project "was still on the first phase" after confetti). 🕐 (was ↷) reschedules the overdue DEADLINE stage's hard due to the tier's next allowed day 9 AM — weekend-proof by construction, no modal needed. Rows name the active stage and, when different, the overdue deadline stage. | The exit-checklist's worst bug. |
| D58 | **(v0.7.0) ⓘ popovers inside <label>s need preventDefault.** A label click re-dispatches to its form control; that second click's target is neither the dot nor the popover, so the handler's else-branch hid the popover the same tick it opened ("the i shows nothing"). ev.preventDefault() in the dot branch stops the forwarding. The Workload dot also gained a hover title for desktop. Fourth platform landmine (D35 hidden, D37 transform, D49 module cache, D58 label forwarding). | |
| D59 | **(v0.7.0) Duplicate for next year.** On project completion (post-fireworks, 2.6s) and via a 🔁 button on every card: modal offers the same window +1 calendar year (Feb 29→28), start snapped FORWARD to the tier's next allowed day, end snapped BACK (never widening; order-guarded). Copies name/color/tier/workload and the project's OWN stage array (surgery survives) with completedAt/dueAt reset; template not consulted (store.addProjectWithStages). | Katie's annual client engagements — "we don't have to remember to repopulate them." |
| D60 | **(v0.7.0) Per-tier ALLOWED DAYS — weekday math is now working-day math.** Every task tier has allowedDays (getDay ints; day toggles in ⚙️ Tiers; missing/empty ⇒ Mon–Fri, so live data needed zero repair). Counted by: stage offsets (addAllowedDays), pipeline windows, deadline fallback, project date interception (a 7-day tier never triggers the modal), decision-modal 🕐 targets, and duplicate snapping. Personal seeds 7-day for new installs; Jake flips his live Personal tier in settings. isWeekend/addWeekdays/weekendNeighbors remain as Mon–Fri wrappers. Stage badges still read "wd" = working days. | Jake: "the personal to do list will have a ton of weekend jobs." |
| D61 | **(v0.8.0) Off-day queue filtering.** When the VIEWED day isn't in a tier's allowedDays, that tier's tasks and project stages don't appear in the queue (or, consequently, the decision modal — Saturday can't nag about Work; Monday brings it all back, overdue glow intact). Project cards, Done-today, Waiting, and anchor events are unaffected. "If she chooses to work, she can look at the project cards or toggle over to Monday." | Jake's smoke-test 2.3. |
| D62 | **(v0.8.0) Duplicate-for-next-year is a REVIEW FORM.** The modal pre-fills but never auto-creates: name (year tokens auto-bumped — full years 2026→2027 AND Katie's YYNN codes resv2606→resv2706, first two digits = start year), start/end (+1yr, snapped to the tier's working days), color, tier, workload — all editable; saving re-validates dates against the CHOSEN tier's days. Stage-level tweaks live in ✎⋮ on the new card, which opens pre-expanded. Snooze row "🕐 Remind me in a day / a week / a month" turns the decision into a real task (project's tier, working-day snapped, 9 AM) — the answer to "right after the fireworks may be too soon" (30d alone was too blunt). **Rev 2 (v0.9.0):** "✎⋮ Review the pipeline first…" edits next year's stage list BEFORE creation — the stages editor is borrowed against a staged copy (S.stagesTarget="@dup"; dup modal hides, returns on Save/Cancel; Cancel discards), so Katie's reflection ("Excel setup should be five steps") happens in the review itself. Nothing writes until Create. | Jake's smoke-test 8/8.1 + clarified intent. |
| D64 | **(css 0.10.0) Rows are container-responsive.** #queue/#waiting-list/#done-list are inline-size containers; a list ≥540px wide renders rows on ONE line (☐ · title+time · ▸ · chip · buttons) by flipping .row-2l back to row-flex with wrap; narrower lists keep the D63 two-line layout; expanded notes always wrap to a full-width line (flex-basis:100%). Container width, NOT viewport — at 1100px the desktop grid gives the queue barely 300px. Browsers without container queries keep two lines (safe fallback). NOTE: the bug that prompted this was almost certainly a STALE DEPLOYED CSS — the screenshots (orphaned checkbox, one-word titles beside sprawling notes, phantom "checkbox" = the ▸ chevron, and Starts/Ends still offset) all match pre-0.8.0 css exactly, while app.js 0.10.0 was demonstrably running. Verify with the header badge tooltip (css version is read live from the loaded stylesheet). | Jake's "wide enough → one row" spec + the smoking-gun that Ends-offset was fixed in 0.8.0. |
| D79 | **(html 0.14.1 + docs) AM/PM + the moved console.** (1) Katie's time picker shows military time with no AM/PM: native `<input type="time">` takes its 12/24-hour cycle from the DEVICE setting in Chromium (incl. Edge mobile) — the app cannot force it there. App-side assist shipped: lang="en" → "en-US" (pins AM/PM in engines that consult page language: Firefox family, some webviews). Primary fix documented in SETUP.md 0.2.5 quick hits: turn OFF the phone's 24-hour format (Android: Settings→System→Date & time; iPhone: General→Date & Time). If Katie's setting is already 12-hour and it persists → escalate to a custom picker (backlogged trigger, not built speculatively). (2) SETUP-PHASE3 Part 3 rewritten (0.1.1) for the merged Cloud Run console: "Write a function" → Node.js tile; service name MUST be lowercase (`pollcalendars` — Cloud Run enforces RFC-1035; entry point keeps `pollCalendars`); env vars live under Containers → Variables & Secrets; auth = Allow unauthenticated/public; inline editor after Create; URL on the service page. **0.1.2 addendum (round 16):** the rest of the create form, field-for-field — Trigger: SKIP (URL is invocable by default; Add trigger = Eventarc, not us); Auth: Allow public access (our secret is the lock); Billing: Request-based; Scaling: Auto min 0 (scale-to-zero = free; ignore the cold-start nudge) **max 1** (no parallelism, caps retry storms); Ingress: **All**, NOT Internal (Scheduler + curl come from the internet), load-balancer box unchecked. | Jake stuck at Part 3 ("no Create function option") + Katie's military-time picker; round 16: the rest of the form. |
| D78 | **(v0.20.0) PHONE CHROME.** Two phone-only adjustments, both living inside the existing 1000px stacked-layout media query (that breakpoint IS "the phone layout" — mid-width windows that stack get the same treatment, which is correct because they share the same scroll pain): (1) header gains a ＋ (#jump-add) that smooth-scrolls to #task-form and focuses #task-title (~350ms later, preventScroll — keyboard up, ready to type); from the year view it setView("day") first since the form lives there. NO modal, per Jake — just the shortcut. (2) The ⏻ mystery-square hides; #signout-bottom ("Log out", in words) sits at the very bottom of #app-screen — where a logout belongs on a device you rarely log out of. Both elements exist in the DOM at all widths but display only under the query; desktop is untouched. | Jake: "the logout button is just a square, and you have to scroll to the bottom to add a new task (a helluva scroll during the week)." |
| D84 | **(v0.24.0) KICK IT DOWN THE ROAD + the settings word-salad.** Jake, having now UNDERSTOOD 🕐 ("Reschedule just kicks it to the next day. I need to change my decision"): decision rows need BOTH the one-tap tomorrow AND an arbitrary future date — "perhaps it was too ambitious to tackle it today." Shipped: 📅 on every decision row opens the due dialog, now GENERALIZED — S.dueTarget is {kind:"stage",projectId,stageIndex}|{kind:"task",taskId}, dueSave/dueClear branch, and clearing a TASK's due shelves it to Waiting (the legit third option: not now, maybe ever). All three prior openDueDialog call sites migrated to the new signature. ALSO: the settings screen word-salad (Jake's screenshot) was D81's field inserted INSIDE cfg-poll's flex .form-row — three children in one row crushed the poll label into a vertical word-stack; the Mirror field now stands alone (html 0.17.2) with flex sizing (css 0.21.1). AND the docs said "⚙️ Settings → Calendar" — the tab is TIMING; SETUP-PHASE3 0.2.1 corrects it. Flexbox's fifth and final scalp of the session, self-inflicted. | Jake: "I need the ability to kick it to the next day OR kick it to a future day/time" + "I don't see a Calendar tab, and Timing is askew." |
| D85 | **(v0.25.0 / queue 0.11.0 / store 0.9.0) CLEAR THE DECK — priority regimes.** Katie's ask: the queue should stop favoring the LEAST-done project once one is far enough along; past a threshold, *just finish it*. Shipped as the D43 **level-3 tiebreaker ONLY** (Jake's option A — expired + chronological still win, so nothing jumps an earlier due date): new `priorityWeight(project, threshold)` returns workload × (pct ≥ T ? pct : 1−pct) — below T more-REMAINING wins (keep abreast, identical to old remainingWork), at/above T more-DONE wins (clear the deck). Threshold = config `clearDeckThreshold` (default 0.6), shown in ⚙️ **Pipeline** as "Clear the deck at N %", fed to queue.js via setClearDeckThreshold on the config subscription (the deadlineHour pattern). Queue item field renamed remainingWork→priorityWeight for honesty; the remainingWork() fn stays as a utility. Live DBs never reseed → readers fall back to 0.6, so it works before anyone opens Settings. 15 real-module unit tests green: regime math, setter guards, and a buildQueue tiebreaker that flips order at T=0.6 vs 0.95 while the earlier-due task stays on top. | Katie: "at some point a project just needs to be finished." Jake: option A, tiebreaker only — "it doesn't matter how close to done something is if something is due sooner." |
| D86 | **(queue 0.12.0) CLEAR THE DECK, GROUPED — D85 corrected before it ever shipped to Katie.** Jake read the test log and caught it: D85 multiplied workload by (pct ≥ T ? pct : 1−pct), which is a **U** — urgency rose at BOTH ends and sagged in the middle, so a 30%-done project outranked a 65%-done one. His model instead: **two piles, pile first.** Past-threshold projects (MOST done first — finish it) sit above ALL catch-up projects (LEAST done first — rescue laggards), however heavy the laggard; workload breaks ONLY an exact-pct tie, heavier first ("the peer review will also take more time, so it needs out the door first"). Four equal projects at T=60% → **95 → 65 → 30 → 40**. Implemented as deckRank() (0=clear deck / 1=catch up / 2=not a project) + compareDeck(); the blended priorityWeight() is GONE, not deprecated. Tasks/events keep rank 2 = sorts last at level 3, exactly as remainingWork:0 always did. Still tiebreaker only. 11 real-module tests incl. Jake's literal four-project case. **Lesson: he asked "is that what you built?" about a one-line test summary and found a design bug in it. Show him the actual ordering, not the adjective.** | Jake: "Let's not do the u — too confusing." |
| D87 | **(functions 0.3.0) THE CARRYOVER — PHASE 3 IS COMPLETE.** D14 built at last, and the design was VERIFIED against §2/§3 + the tier tooltip rather than inherited: `midnightCarryover` is a TIER flag (seeded ON for Work/Family), missed tasks get "❗ <task>" on today's calendar at config.carryoverWriteHour (default 9), tomato colorId 11. **Two findings that reshaped Otto's guessed shape** (§4 said "likely localHour === carryoverWriteHour"): (1) the sleep gate (22–6) returns BEFORE job routing, so a midnight trigger would have been skipped every night forever; (2) it needs NO hour trigger at all — "due before today began" is true whenever it runs, so the first waking tick (~06:07) writes the 9 AM landing with three hours of lead, and no cron hiccup, outage, or cadence change can silently skip a morning. Hour-gating would have been one hiccup away from a lie in the app's core promise. **Hazard designed around:** the mirror queries tcApp=tentacalendar and keys its ledger by tcTaskId — shared tags would have given it two events per task and it would have patched the ❗ back to the honest due time. Carryover owns tcApp=tentacalendar-carryover + tcCarryKey=taskId#YYYY-MM-DD; same calendar, mutually invisible. Reconciles TODAY only (mirror idiom): create / re-time if the hour moved / delete once done-or-rescheduled; earlier days never touched. 25 unit tests on selection, boundaries, landing, idempotency, hour-independence. SETUP-PHASE3 0.3.0 Part 8 + smoke WW1–WW6. | Otto's reserved opening quest, claimed. |
| D88 | **(v0.27.0 / queue 0.13.0 / css 0.22.0) PHASE 4 BEGINS — THE WEEK VIEW EXISTS.** Jake: "combine the calendar sync and the tasks, with the projects and the day long events across the top" — and "if we don't like it, we can always add a second one" (the Annual/Months/Timeline precedent: multiple views beat one compromise). **Chosen shape: seven QUEUE columns, not a time grid.** Reasoning, on the record: (a) a task due at 4 PM is a DEADLINE, not an appointment — a clock gives it false precision; (b) every computed stage deadline lands at deadlineHour, so a grid paints a wall of 4 PM collisions that don't exist; (c) buildQueue already owns a correct day, so 7 calls inherit expiry/off-days/D86 piles for free and the risk collapses to layout. **Counter-argument logged for the future second view: a time grid shows WHERE THE FREE HOURS ARE, which a consultant may well want — build it as a sibling, not a replacement.** buildWeek() adds only what exists ACROSS days: projectSpans (bars + stage pips) and bannerSpans (one bar per all-day event, not D80's pill-per-day). **KEY FINDING: projects must NOT repeat down the columns** — D48 rides a project through its whole window, so a 3-month project would render in all 7 columns; a stage row now lands only on its deadline day (plus today if expired), and the span goes up top. Per-day LOAD BAR (count + normalized fill, red if anything's missed) = the one thing a week says that a day can't: "Thursday is going to hurt, and it's Monday." Rolling today+6 default (a kiosk shouldn't spend four columns on history every Friday; missed things still shout in TODAY's column via D43 expiry, so nothing is lost), ◀ ▶ pages, "back to now" restores rolling. View toggle is now a 3-CYCLE: day → week → year, glyph shows the DESTINATION. **addDaysLocal() added: ts + DAY_MS repeats a date across DST fall-back** (Nov 1 00:00 + 24h = Nov 1 23:00) — the week would have shown Nov 1 twice and dropped Nov 7, twice a year, silently. 37 unit tests incl. both DST weeks. Read-only v1 (drag deferred — D72/D73 machinery is there when the layout's proven). Sized for a 55" 4K wall at quarter width (~137px columns): clamp() type, no hover-only info, min-width:0 on every text-bearing flex child. | Jake: "it will be large, and it will be in charge." |
| D89 | **(v0.28.0 / queue 0.14.0 / css 0.23.0) GANTESQUE — Jake's five, from REAL data.** The screenshot did what no mockup could. (1) **Project bars were alphabetical** → now sort in the queue's own language: expired first, then soonest deadline, then the D86 piles. Ten bars A–Z said nothing about what to look at. (2) **Dates were stranded halfway down** under the strips → own header row at the top, where a calendar puts them. (3) **THE LOAD BAR WAS A LIE OF FORM:** it encoded "this day has 40% as many items as the busiest day" in the exact visual language of "you are 40% done." Jake: two items, one overdue, one due in 20 min, none done, bar 40% red — "we're unclear as to what that means." Correct response was DELETE, not tune: replaced with words ("2 left · ❗1"). **Lesson: a chart that must be explained is a chart that's wrong; a number that means what it says beats a bar that doesn't.** (4) **Stage pips were invisible at 7px** → the deadline DAY is now a highlighted block aligned to its column (Jake's own design: "highlighted blocks when there's a specific deadline on the day"). (5) **The cycling view glyph needed a hover to explain itself** = a failed control → labeled 3-way switch (📋 Today / 🗓️ Week / 📅 Year). ALSO, found by test: **an EXPIRED project whose pipeline window is entirely past would VANISH from the board** — the week filter dropped it while D48 keeps it alive on today. A missed project disappearing from the planning view is the exact inverse of the app's promise; late bars now stretch to today. Week-start modes shipped: Today+6 / Sun–Sat / Mon–Sun, remembered (tc-wmode) — **Jake corrected my "fixed weeks waste columns on history": "a part of it is also reflecting on the week to work on next week."** CSS 0.22.0 shipped with a stale 0.21.1 banner (--tc-version was right, so the badge and Jake's deploy were fine) — caught by Jake, not by my ship-check; the banner grep now belongs in it. | Jake's screenshot + "Gantesque?" |
| D90 | **(v0.29.0 / queue 0.14.1 / css 0.24.0 / html 0.22.0) THE BOARD MAKES ROOM.** Round 2 of real-screenshot feedback. **HEIGHT was the blocker** — 11 project bars pushed the day columns clean off the screen. #wv-sizes (Auto/▮/▪/▁/🧵) + Auto stepping down by count (≤4 full, ≤8 half, ≤14 quarter, else hair) + a 46vh cap on the strips: the DAYS are the board's job, the bars are context. **Banner packing:** Jake and Katie both spotted a 2-event Friday claiming a 3rd row — CSS grid's default auto-flow is SPARSE and never walks the cursor backward, so once DnD forced row 2 every later span started looking from row 2 while row 1 cols 6–7 sat empty. `grid-auto-flow: row dense` = one word, two rows. **My own bug:** `.wv-dl { pointer-events: none }` killed the very tooltip I'd attached to the deadline block — Jake asked for a hover that I had written and then disabled. Rows + bars are now clickable → openDueDialog ("not gonna do it today? Can click on it to reschedule it"), hovers carry the untruncated name + due + notes (137px columns will always truncate; "Chec…" is not actionable). Events are hover-only — they come FROM Google Calendar and this app doesn't own them. **NAV-SLOTS, all three views (app-wide design flag from Jake):** fixed-width slots either side of the label mean ◀ ▶ NEVER move, and "back to now" sits on the side matching its direction of travel (in the future → by ◀, since going back is leftward). Three clicks forward is now three clicks forward, not forward/back-to-now/forward. Caught by my own census: app.js referenced p.activeStageIndex which queue.js never emitted — the clickable bar would have opened the dialog on undefined. | Jake: "height was a real problem." |
| D91 | **(v0.30.0 / css 0.25.0 / html 0.23.0) THE BOARD ACTUALLY MAKES ROOM — and a process failure worth more than the fix.** Jake: the Bars buttons "neither light up, nor change anything," and Auto doesn't thin. **Cause: D90's density edit NEVER LANDED.** The patch script built a list of replacements, applied three by hand, and never iterated the list — so `weekBarSize`/`setWeekSize` were CALLED but never DEFINED and `strip.dataset.size` was never set. The script printed "app.js render ok" and I believed it. `node --check` PARSES; it does not notice a call to a function nobody wrote, and my ship-check greps verified DOM ids and CSS classes but never that the functions I call EXIST. See meta-rule 9. **Two real bugs underneath:** (1) `#week-view { height: 100% }` resolved against `#app-screen`, which has no height — so 100% → auto, the section grew to its content, `#wv-grid`'s flex:1 had nothing to divide against, and the columns walked off the bottom. Now pinned by fitWeekHeight() from the space under the header (offsetTop is stable — it depends on the header, not on us, so no feedback loop) + a resize listener. (2) Auto laddered on COUNT alone, which says nothing about whether the bars FIT — now it measures the board and picks the fattest size that leaves the columns ~60%+. Same 11 bars: full @2000px, half @1000px, hair @530px. Monotonic both ways, tested. **🧵 is the floor**, so enough bars still overflow (50 hairs want 700px) — CSS `max-height:38% + overflow-y:auto` is the real backstop, and it only works BECAUSE fitWeekHeight() gives the parent a definite height (a % of `auto` silently does nothing). **Expand-on-click (Jake):** at 🧵/▁ the first click gives the words back (.forced-full, allowed to overflow its slot), the second acts. Read, then commit — never make someone act on a bar they can't read. | Jake: "clicking the four height buttons doesn't do anything." |
| D92 | **(v0.31.0 / css 0.26.0 / html 0.24.0) CLICK TO READ, CLICK TO FOLD.** D91's expand was one-way — Jake could open a bar and never close it. His proposed fix: branch on whether the native tooltip had appeared (click-before-hover = expand, click-after = edit). Rambo pushed back and Jake took it: hover-timing makes a click mean different things depending on how long you paused over it, and a planning board can't afford that surprise. **One rule instead, no timing: "if you can read it, clicking acts; if you can't, clicking makes it readable."** Compact (▁/🧵) → click TOGGLES expand/collapse, and the expanded bar grows a ✎ to act. Readable (▮/▪) → click acts directly. The hover text says which one you'll get. | Jake: "there's no way to collapse it back down." |
| D93 | **KATIE'S CORRECTION — a task time is a DUE date, not a start time. This inverts the clock-grid design before it was built.** Rambo's D91 diagram drew a task's runway extending FORWARD from its time to midnight; that is backwards. A task due at 6 PM can be done any time BEFORE 6, so its block ENDS at 6 and the available time trails BACKWARD; the red overdue state is what extends forward past it. **This also promotes "estimated time to complete" from a nice-to-have to the load-bearing field it always was:** with it, a task is a real block `[due − estimate, due]` with a real length, and dragging its top edge IS estimating — which is exactly the "can I fit dinner on Tuesday?" question the week view exists to answer. Without it, a task can only ever be a line at its deadline. **Do not build the clock grid on the D91 diagram — build it on this.** | Katie, via Jake: "tasks times are DUE dates, not start times." |
| D94 | **VERSIONING, PROPERLY DEFINED (Jake) — supersedes the old x.y.z gloss for this project.** The old rule said patch=bugfix / minor=feature / major=breaking. Jake's is better because it makes the number MEAN something: **Z = "it's not working the way I want"** — which INCLUDES refining a feature already agreed on. Tweaking bar height, fixing alignment, making the Bars buttons work: not features, but the bug of an old idea not landing yet. D89–D92 were all Y bumps; every one should have been Z. **Y = something is here that wasn't here before** (firstDueAt earns one; the week view itself did). **X = the usefulness fundamentally changes**, not "something broke" — the week view alone didn't earn it, the 1.0 kiosk (when the pieces become one thing) does. **NOT RENUMBERED RETROACTIVELY:** the badge exists to tell Jake what's RUNNING, and a deploy reading 0.24.2 while 0.26.0 sits on his screen would look like a stale cache — breaking the one deploy-verification mechanism the project has. Shipped numbers stand as the record of what he ran; the rule binds from here. | Jake: "Y is for features, not figuring out features we already have. That's what Z is for." |
| D94.1 | **(v0.31.1 / css 0.26.1 / html 0.24.1 — a Z, per D94) SPEND THE GLASS.** Jake's screen math was inverted: a QUARTER of a 4K screen is a whole 1080p screen, so there was room to spare and Auto was hoarding it. Reframed: Auto was never "make the bars small," it's "split the glass" — and only the human knows how they want it spent. **#wv-split**: drag the projects/days boundary (10–75%), double-click resets to 34%, persisted (tc-wstrip); Auto then refits the bar size into whatever share is left, so the two controls compose instead of fighting. **Alignment bug:** grid's default align-items is STRETCH, so a title span filled its area and its text sat at the TOP while .wv-when was align-self:center — two font sizes, two vertical anchors, one visible misalignment. `align-items:center` on .wv-row; with a sub-line the time still centres across both rows. | Jake: "all that wasted space at the bottom." |
| D95 | **(store 0.10.0 / app 0.32.0 / css 0.27.0 — a Y, per D94: this is genuinely new) TASKS REMEMBER BEING MOVED.** `firstDueAt` (the original commitment) + `rescheduleCount`. Jake: "if she reschedules something 5 times, that's worthy of looking at the _why_." Until now a reschedule overwrote dueAt and **erased its own evidence** — the exact thing worth reflecting on. **Counted inside store.updateTask(), NOT at the call sites** — so all three existing dueAt paths (the D84 due dialog, shelving to Waiting, the decision modal's 🕐) are caught, and so is the drag-to-reschedule that doesn't exist yet: a future caller cannot forget to count. Costs one getDoc per date change; rescheduling is rare. **NO MIGRATION AND NO BACKFILL BUTTON** — `firstDueAt ?? dueAt` at read time IS the retroactive answer (a task predating the field has its current due as its first KNOWN due: honest, zero writes to Katie's live data). Jake asked whether backfill was possible; the fallback is the backfill. Exported as `taskFirstDue(t)`. **Escalation cannot inflate the count** — it only re-times the queue's display slot, never dueAt, so only a human moving a date counts. **Semantics the tests forced (worth keeping):** (a) you can only MOVE a date that existed — null→date is SCHEDULING, not rescheduling, and counting it would inflate the number with a non-event; the count's whole worth is that a 5 means something; (b) a no-op save of the same date isn't a move; (c) shelving to Waiting DOES count (D84's "not now, maybe ever" is a deferral) but picking it back up doesn't, so a shelf round-trip is one deferral, not two; (d) firstDueAt FREEZES at the first commitment and is never overwritten. 33 unit tests across both cuts. Surfaced in the week hover ("↻ moved 4× · first due Jul 10") and a dotted left edge at 3+ moves — dotted, not red: a pattern to notice, not a scolding. The victories/put-offs cards read from this. | Jake: "I think that count is great, too." |
| D96 | **(app 0.33.0 / html 0.25.0 / manifest.json 0.1.0 + 4 PNG icons — a Y) ON THE WALL: the app is installable, and ⛶ finally exists in Today.** Jake got the page running on an old Android tablet and needs it fullscreen. Three routes, and they stack: **(1) manifest.json** — `display: standalone` with `display_override: [fullscreen, standalone, minimal-ui]`, so Add to Home Screen kills the address bar everywhere and asks for true fullscreen where it's supported, falling back quietly on old Android (which is where the tablet lives). Real PNG icons (192/512/512-maskable/180-apple): the existing favicon is an inline SVG data-URI 🐙, which old Chrome will NOT accept as a manifest icon. Icons render the SAME 🐙 emoji via NotoColorEmoji so they match the favicon rather than being some hand-drawn approximation; maskable keeps the art in the inner ~78% so a circle-cropping launcher can't guillotine it. **(2) Android screen pinning** needs nothing from us — pure device setting. **(3) ⛶** — Today had none at all; week and year each carried their OWN copy of the toggle, which is exactly how the third one never got written. Now ONE `toggleFullscreen()` for all three. **The webkit* fallbacks are the point, not decoration:** year's copy called `requestFullscreen()` unguarded (THROWS a TypeError on a browser without the unprefixed API) and week's used `?.()` (fails SILENTLY — the worst outcome for a button whose whole job is to visibly do something). Now: try prefixed, and log a real reason if the browser refuses. | Jake: "I need it to run full screen ... on an old android tablet." |
| D103 | **(app 0.38.0 / css 0.31.0 / html 0.28.2 — a Z) THE TRACKS FIX + AUTO MEASURES REALITY.** Jake, from two screenshots. **(a) Tidal's compressed past columns misaligned the DAY HEADS.** Cause: the week's seven columns were declared in TWO places — `repeat(7,1fr)` in the stylesheet for the three .wv-strip rows, and inline JS on #wv-grid for Tidal's 0.55fr compression. That is not a shared number; it's two numbers that agree until one doesn't. **I wrote the warning myself in D100** ("a rail would knock every strip out of alignment") and then walked into the same hazard by another route. Fix: tracks are ONE CSS var `--wv-tracks` on #week-view, read by #wv-grid AND every .wv-strip. They cannot disagree. **(b) The Clock wrapped to 3 rows at half a 2K screen** — that's the pre-existing phone rule `@media (max-width:900px){repeat(3,1fr)}`, fine for text-list layouts, ruinous for the clock: three rows of three days = three unrelated time axes, and hour labels only render at `i===0`, so rows 2–3 had NO axis. The clock now opts out (`#week-view.l-clock` — two ids + class beats one id) and stays 7 narrow columns, because a clock column is shapes, not text. **Using a VAR rather than an inline style is what makes this work**: the media rule is later + equal specificity so it still WINS for Gantesque/Tidal, where an inline style would have bulldozed it. The layout class had to move to **#week-view** — the strips live outside #wv-grid, so CSS couldn't reach them from there. **(c) Auto under-shrank: its budget was fiction.** It modelled the strip box as `board × 34%`, but (i) my D97 `#tidal-horizon` is `flex:0 0 auto` (never shrinks) while `#wv-strips` is `flex:0 1 auto` (always does), so the horizon takes its height off the top and the strips silently get less than their share; and (ii) `weekBarSize(w.projectSpans.length)` never counted the BANNER rows sharing that same box — predates D97, the horizon just made it visible. Auto now MEASURES: `(board − horizonH) × pct − bannersH`. Banners render before projects, so they have real layout by the time we ask. **Standing lesson: any geometry declared in two places is a bug with a delay on it.** | Jake: "doesn't resize the day headings at the top, so they no longer align… Auto also didn't make all of them fit." |
| D102 | **(rules 0.2.0 / store 0.11.1 — the security pass Jake flagged in his FIRST message).** Two changes, one real. **`email_verified` is the actual hole and nobody had named it** — Gemini's list walked right past it. `request.auth.token.email` is a CLAIM. Today only the Google provider is enabled so every token is Google-vouched and the check is a no-op; the day anyone enables Email/Password in the console — two clicks, looks unrelated to security — a stranger could register Katie's address, never verify it, and the rules would hand them the workspace. One line, closes a trapdoor that opens BY ACCIDENT. **`.lower()` is belt-and-braces, NOT a live bug**: Gemini's stated rationale (Google returns "unpredictable casing") is false for @gmail.com — it returns the canonical lowercase address, and months of Katie writing daily is the proof. Taken anyway because custom domains carry whatever an admin provisioned and §8.4 doesn't stay two gmail accounts. store 0.11.1 lowercases the CLIENT allowlist to match: that list is not security (the rules are), but if the two disagree it breaks as a login bug — client stricter = "bounced back to sign-in", rules stricter = "insufficient permissions". Symmetry or nothing. | Jake, turn 1: "some security issues you may or may not agree with — which we can talk about." |
| D101 | **(app 0.37.0, folded into the 0.38.0 drop — a Y) THE GESTURE LOCK.** Gemini's one real find: the 60s tick nukes the DOM mid-drag. **Its proposed fix was dangerous** — `if (dragging) return` in render() would DISCARD a live update, not postpone it, because **render() is a Firestore SNAPSHOT HANDLER** (subscribeTasks et al. all call it), and silent data loss in a live-sync accountability app is worse than the bug. Defer + flush on release instead. **Scope, honestly: the drags already SURVIVE mid-render by design** — listeners on document (D73), ghosts body-level fixed precisely so the tick can't hide them (D74) — so the real symptom is cosmetic (the .dragging dim resets). **SAFETY VALVE, and it's the important part:** a lost pointerup would leave the lock stuck and silently freeze live sync forever while the app looked healthy — the worst failure this app can have. tick() force-releases any gesture older than 30s. It **fails OPEN**. Covers the year bar drag and D100's estimate drag (mine had the same exposure). The splitter needed nothing — `bar.dataset.wired` already guards its re-bind (checked, not assumed). | Gemini, correctly; the fix, not. |
| D100 | **(app 0.36.0 / queue 0.16.0 / store 0.11.0 / css 0.30.0 / html 0.28.0 — a Y) THE CLOCK GRID — layout #3, and §5a's last open item on the week.** Built on **D93, not D91**, which is the entire design: a task time is a DUE date, so a task is the block **[due − estimate, due]** — the runway ENDS at the promise and trails BACKWARD. D91 drew it forward to midnight, which says "start this at 6 PM" when the truth is "have this done by 6". **Only OVERDUE runs forward**, deadline → now, as a red hatched tail — the one thing on the grid that's really growing. **Events own [start,end] outright** (D93/D80): nothing inferred, no grip, solid edge. **Stages** get the deadline shape at the default length (they carry no estimate field; inventing one would be a lie). **estimateMinutes ships (store 0.11.0)** — D93 promoted it from nice-to-have to load-bearing, and `addTask` destructures explicitly so it would have been silently DROPPED. **null ≠ 0**: blank means she never said, and those blocks render DASHED at 30 min with the tooltip admitting it. `updateTask` needed nothing (D95 only special-cases dueAt), so **estimating never touches the reschedule count**. **DRAG THE TOP EDGE = ESTIMATE** (D93's payoff): document-level listeners so the minute tick can't orphan the gesture (D65/D73), the block itself is the preview (D77 deleted the second-preview pattern), re-derived from the SHARED axis each move rather than read back from its own pixels (which drifts). **NO GUTTER COLUMN** — .wv-strip is a 7-track grid and projectSpans position by grid-column, so a rail would misalign every strip from the days; hour labels ride inside column 1. **Shared axis** (weekClockWindow) fits the week's real content to whole hours with a 6-hour floor, else seven unrelated charts. Lane packing is **per overlap-cluster, not per day** — one 3-way 9 AM pileup must not shave every other block to a third. The red tail occupies its lane (a blown deadline can't hide behind the next thing). Midnight clamp keeps a 00:15 deadline's runway in the column but marks it (⌃ dotted top). Reflection's toggle hides in Clock: a finished task has no runway to draw. 30 geometry tests. | §5a: "if an event is scheduled for 6 PM today, it should show up at 6 PM today." Katie, D93: "tasks times are DUE dates, not start times." |
| D99 | **(app 0.35.1 / css 0.29.1 / html 0.27.1 — a Z) THE DEAD SPACE.** Jake's first screenshot of D98: past columns showed a field of nothing with the cards pinned at the bottom. Cause: `.wv-list { flex: 1 }` — an **empty** list still claims the column, and a past day whose items are all done/moved HAS no items, so an invisible box held the glass and `.refl-cards.in-columns { flex: 0 0 auto; max-height: 45% }` got stranded beneath it. **The bug appeared only on the exact columns reflection exists for**, and today (which still has items) looked correct — which is why it survived the ship-check. Fix: JS adds `.cards-fill` to a column with cards and no items; CSS lets the list collapse to 0 0 auto and the cards take `flex: 1 1 auto` with the cap lifted. No `:has()` — the target is still an old Android tablet (D96). **ALSO: the ship-check ate its own tail.** It asserted `html.includes('app.js?v=0.35.0')` as a LITERAL, so bumping to 0.35.1 made the CHECKER fail while the files were right. Now every `?v=` is DERIVED from the imported file's own declaration and compared — a literal in the checker is just one more thing to forget to bump. Third time this exact shape has bitten (D89 banner, D97 app.js banner, D99 checker literals): **anything hand-written per-file is a hole; iterate and derive.** | Jake: "Why is there all that dead space between the top and the reflection at the bottom?" |
| D98 | **(app 0.35.0 / css 0.29.0 / html 0.27.0 — a Y) GANTESQUE IS DONE.** The 5a-bis victories/put-offs cards render in **GANTESQUE**, which is where they were owed. D97 built the engine and then scoped the cards to Tidal — so the 12-turn-old Gantesque debt stayed open AND the two layouts became un-comparable, which is the one job Tidal has. Jake, out of patience and correct: "the tidal is supposed to be something that I can compare to the gant. Just get gant done." **The cards were never a Tidal feature; they're the week's** (decided round 7). `reflectionCards()` is layout-agnostic (renamed from tidalCards; CSS `.tidal-*` → `.refl-*`), the engine stays `dayReflection()` in queue.js, and each layout only chooses WHERE to hang them: **Gantesque ADDS the cards under its day list (the list is still the day); Tidal REPLACES the list with a Wake.** That difference is now the actual comparison — same data, same engine, two answers to "what is a past column for." Toggle shows in both. **Also: past day heads stop lying.** They said "N left" — a FUTURE word on a day that's gone, with `missed` permanently 0 back there (expired is viewingToday-gated, D97). Past heads now read ✓done / ↻putoff; live days keep D89's words. **LESSON FOR SUCCESSORS, the real one:** an outstanding item does not get to ride into a shiny new build as a sub-feature of it. The debt was "the cards"; I shipped "the cards, inside Tidal," which is not the same object and left the ledger open while looking closed. Check what the OWED thing is, deliver it where it was owed, THEN build the new thing. | Jake: "Just get the gant done, Billye. Just get gant done." |
| D97 | **(queue 0.15.0 / app 0.34.0 / css 0.28.0 / html 0.26.0 — a Y) THE TIDAL GRID + REFLECTION.** Two things that turned out to be one thing. **(1) The cards (5a-bis) — Rambo shipped D95's data layer and the HANDOFF said "what remains is only the CARDS"; the cards were never built.** Jake didn't assume, he asked me to check. They exist now. **(2) Gemini's Tidal Grid**, built from scratch against the same history and arriving independently at 5a-bis: its "Wake" for past days IS the put-offs card. So the reflection engine serves both. **Chosen shape:** `dayReflection()` in queue.js scans the RAW task/project arrays against a day — it cannot read `col.items`, and that's the whole finding. **THREE VERIFIED FLAWS IN THE BLUEPRINT, each of which ships an empty box that looks like it works:** (a) `expired` is gated on `viewingToday` by design, so every past column's items are `expired:false` — the specced `.expired-ghost` filter renders NOTHING, forever; (b) a task moved off Tuesday **is not in Tuesday's items at all** (buildQueue filters on the CURRENT dueAt), so the addendum's "your primary technical challenge is pure DOM manipulation" is false — the evidence lives only in firstDueAt and queue.js must emit it; (c) **the addendum's `firstDueAt` reads are the live-data trap**: D95 only writes firstDueAt on a task's first reschedule AFTER deploy, so every task in Katie's database has `firstDueAt === undefined` and the specced Wake would be empty until she happened to move something — `firstDueOf()` uses D95's `?? dueAt` fallback, which IS the backfill. Also (d) `.wv-col.is-past { flex-basis }` is inert: #wv-grid is CSS **Grid**, so compression had to come from computed track sizes. **PUT-OFFS HAVE THREE FLAVOURS, and conflating them teaches nothing:** moved (she decided, picked a date), shelved (she decided, picked none — D84), ignored (nobody decided anything; the only one that's derivable without D95, and the one that stings). **The Wake shows BOTH halves** — Gemini's summary counted only what was planned and dropped; Jake asked for "what she got done, what she didn't." A card that only counts failures is a scolding, not reflection. **Compression composes instead of fighting:** cards OFF → past columns shrink to 0.55fr and hand their glass to the live days; cards ON → they get it back, because D89 says the past columns ARE the reflection (the D94.1 split/Auto pattern). **The Horizon separates two animals:** a follow-up (D4) can't be scheduled — it materialises on its parent's completion — while a shelved task is real inventory; only the second is clickable, the first reads as inert (⛓). **Deliberate cost, on the record:** the anchor/flow split BREAKS D43's chronological interleave (a 9 AM task no longer sits visibly above a noon meeting). That IS the layout's thesis — events own time, tasks own a deadline (D93) — and the reason it's a sibling, not a replacement. Columns keeps the interleave. 31 real-module tests. **PROCESS MISS, caught by Jake pre-deploy (he refused to install until it was confirmed — the right call):** app.js shipped with a 0.33.0 banner over an 0.34.0 APP_VERSION. This is D89 verbatim, one file over: my ship-check grepped banners for css, html and queue and not for app.js, which is the D91 lesson too (a hand-built list of checks is a hand-built list of holes). Fixed, and meta-rule 6 now demands the banner check be MECHANICAL — iterate all seven files, assert banner === declaration. All seven now agree. | Jake: "Gantesque is okay, but it's not hitting the way the other views have. We want to try other options." |
| D83 | **(v0.23.1) THE CLOCK SAYS WHERE + a lag under watch.** Jake ran his queued decision-modal live test and hit two things. (1) NOT A BUG, ILLEGIBLE DESIGN: 🕐 IS the reschedule — one click pushes the deadline to the tier's next allowed day 9 AM (D52/D60); the row dropping / modal closing is SUCCESS. But the target lived only in a hover tooltip (phones have no hover) → the sub line now reads "N days overdue · 🕐 → Mon, Jul 13 9 AM". No picker, by design. (2) **UNDER WATCH, DO NOT THEORIZE (meta-rule 1):** ✓ in the decision modal took ~30s to remove the row — latency compensation should make it instant; ~30s smells like the minute tick doing the snapshot-render's job, i.e., a write path whose onSnapshot→render didn't fire. UNREPRODUCED here. **Successor's diagnostic on next occurrence:** (a) did the QUEUE row update instantly while the MODAL row lagged (isolates renderDecision vs the whole pipeline)? (b) console errors? (c) was the device offline-ish? Capture before touching code. (3) PRESENTATION MISS logged: round 21's mirror deliverables (SETUP-PHASE3 0.2.0 Part 7, functions 0.2.0) were built + ship-checked but never PRESENTED — Jake couldn't find the mirror steps because they never reached him. Lesson: the turn isn't done until present_files runs. | Jake: "if I click reschedule, the modal just closes… shouldn't it give me a modal?" + the 30-second ✓. |
| D82 | **(v0.23.0 + queue 0.10.0) TIME PASSES, FOLLOW-UPS FLEX (Otto's finale).** (1) Timed events whose window has passed — end, or start+1h grace for endless events — sink into a dimmed EARLIER TODAY box (struck times) instead of loitering in the live queue. TODAY ONLY: viewing a past or future day shows everything in place (a past day is all "passed"; the split would be noise). Church at 4:49 PM was the exhibit. (2) Follow-ups get a real dialog (#followup-modal — the prompt() pair is RETIRED, closing a 5c backlog item) with amount + minutes/hours/days/weeks. Storage remains offsetDays but FRACTIONAL — store.js line ~200 materializes by multiplication, so 45 min = 45/1440 days works with store.js UNTOUCHED (it retires at 0.8.0, never changed since Phase 1). Waiting rows read "+45m/+2h/+3d/+1w" (fmtOffset/splitOffset round-trip; 90m stays 90m, not 1.5h). BUILD WAR STORY for successors: this round hit BOTH classic traps in one hour — a duplicate identifier (queue.js already had viewingToday; node --check parses scripts and missed the module-scope collision — the ESM import in the test caught it) and a phantom helper scare (fmtTime "missing" — it's a queue.js export, grep the right file). Both caught pre-ship by testing against the real module. | Jake: follow-up counts need units; "Church still waiting there patiently at 4:49 PM." |
| D81 | **(functions 0.2.0 + v0.22.0) THE MIRROR — Phase 3 step 2.** Jake asked "push to gcal every night at midnight?" → shipped fresher and safer: HOURLY RECONCILE on the poll's existing heartbeat (one service, `?job=poll|mirror|all` routing; Scheduler URL gains `?job=all`). Rules: dated+incomplete tasks → 30-min events at HONEST dueAt (no escalation theater) on a DEDICATED calendar (cfg.mirrorCalendarId, new ⚙️ field); complete/delete/undate → event removed next sync; Waiting never mirrors. Sync ledger = the calendar itself: events tagged extendedProperties.private.tcTaskId + tcApp (listed via privateExtendedProperty filter) — NO task-doc writes, no schema/rules changes; the reserved mirroredGcalEventId field is formally UNUSED. **LOOP GUARD (the thing to worry about):** mirroring into a polled calendar would boomerang every task back as its own anchor — the function refuses any mirrorCalendarId matching a tier's gcalCalendarId, and the settings hint warns. Scope widened readonly→calendar (rw). Report shape changed: results nest under `jobs` (SETUP examples updated). Reconcile decision matrix unit-tested (create/patch/prune/empty + guard). | Jake: "A tentacalendar push to a gcal instance, right? Every night at midnight? Or is there something else I have to worry about?" — there was: the loop. |
| D80 | **(v0.21.0 + queue 0.9.0) EVENT TAXONOMY — banners vs anchors.** Jake, pre-integration, with the family calendar in hand: timed events are "things to go to," all-day/multi-day events are "things that are happening" — not tasks, no check-off. Design confirmed + gap closed: (1) TIMED events were ALWAYS checkbox-free anchors (D3) — queue rows at their slot, pinned in the lead window (that IS the in-app notification), aging off at midnight, no acknowledgment ritual. Checkboxes are for work; if an event spawns work ("pack the camp bag"), that's a task the human creates. (2) ALL-DAY events (the gap: they'd have spammed every day as 12:00 AM rows) now split into a BANNER STRIP above the queue: tier-tinted pills, "Zoo Camp · day 3/5" span math (all-day ends are EXCLUSIVE per Google — first queue.js change since 0.8.0), hover = full date range, no pin/expiry/checkbox, hidden-tier aware. Behavior decided by the event's OWN shape (the poll's allDay flag) — no per-event config in v1; the family calendar changes NOTHING about how it's authored. Tested against the real module: day-N math, exclusive ends, pins intact, hidden tiers, single-day 1/1. | Jake: "My parents are in town, but that's not a task… I guess there's more here to figure out than I thought." There was, and it was mostly already figured. |
| D77 | **(v0.19.2) THE GHOST IS THE PREVIEW.** Jake: drags commit right but the chip "strays into May first." Verdict: the D73 pointer-following chip was noise once D74's ghosts existed — two previews, one less truthful. The chip RETIRES: during a drag the original bar dims in place (.dragging → opacity .35), and the ghosts — now TINTED in the project's own color (background hexToRgba(color,.4), borderColor color) — are the entire moving preview. The transform/width preview code (source of the D74 and D76 visual bugs) is deleted outright; wireBarDrag's onMove now only computes ns/ne, paints ghosts, updates the label. ALSO ANSWERED + SETUP.md 0.2.3: badge showed css 0.17.0 while GitHub had 0.18.0 — YES the badge still reads --tc-version from the APPLIED stylesheet (that's the point); the browser cached stale content UNDER the new ?v= URL (deploy-race poisoning, the D64 pattern). Hard-refresh evicts; new troubleshooting bullet documents the symptom. | Jake: "it ends up where it's supposed to, but it strays into May first" + "are we no longer grabbing the version from the css file itself?" |
| D76 | **(v0.19.1) THE RESOLVER LEARNS LEFT FROM RIGHT (bugfix, Jake's mid-drag screenshot).** Two bugs, one root: D72 put months SIDE-BY-SIDE, and D73's date resolver measured only VERTICAL distance to rows. Rows across Apr|May|Jun share y-bands → for a pointer inside June, April's row won (dy=0, DOM order), x clamped to April's right edge, every horizontal position resolved to the SAME date → dDays=0 → horizontal drags dead while vertical ±7s worked (Jake could hit Jun 5 but not Jun 4/6/11/13). Fix: true 2D distance (ddx²+ddy²; inside a rect = 0 for that rect alone). Second bug: ghosts clipped to weeks but not MONTHS — May's spillover week (May 31–Jun 6) shares its window with June's week 1, so Jun 5–6 lit up in BOTH blocks ("the dotted line goes into May"). Fix: lanes carry data-clip-s/e (week ∩ month in calendar layouts, full row in timeline); ghosts intersect the clip, matching exactly where bars render. TESTING LESSON recorded: the D73 unit tests stacked rows vertically only — they were structurally blind to side-by-side geometry; the D76 suite's primary regression IS the side-by-side map, all 7 June columns asserted day-by-day. | Jake: "I can't drag horizontally at all anymore! Danger Will Robinson!" — plus the priceless mid-drag screenshot. |
| D75 | **PHASE 3 GREENLIT + Jake's 5a answers (all three).** (1) Cloud Functions order CONFIRMED: GCal poll (pull) → task→GCal mirror (push) → 9 AM carryover writes. (2) The Annual/year views are PROJECT PLANNING ONLY — no tasks or calendar events there for now; future idea (backlogged): toggleable overlay calendars (e.g., federal holidays), gated behind calendar integration. (3) Billable hours slots AFTER the phase-4 dashboard ("the full view"). SHIPPED (step 1): functions/index.js 0.1.0 — pollCalendars, plain functions-framework style ON PURPOSE (console inline editor expects it; Jake has no CLI), HTTPS + x-poll-secret header, TZ=America/Chicago env (makes all-day midnights and sleep-hour checks Nashville-local), service-account auth via calendar SHARING (no OAuth), per-tier full-replace sync into eventsCache (idempotent; one bad tier can't starve the rest; batches chunked at 450), 1-day lookback / 60-day horizon, sleep-hours skip with ?force=1 bypass. Cloud Scheduler at :07 hourly holds the real cadence (config.pollIntervalMinutes is informational for now). ZERO client changes — subscribeEvents has watched eventsCache since v0.1.0. SETUP-PHASE3.md 0.1.0 = browser-only walkthrough (Blaze + $5 budget alert, 4 APIs, console paste-deploy, robot-email calendar sharing, curl test, Scheduler). | Jake: "Otto, if you're good for phase 3, then I am." |
| D74 | **(v0.19.0) DROP GHOSTS.** Jake: cross-row drags COMMIT right but the preview showed nothing at the target ("does not light up Jun 5 at all"). Now every pointermove renders dashed accent slots (.yv-ghost, body-level position:fixed, z-index 50, pointer-events:none) in EVERY row the pending [ns..ne] crosses — the landing zone is visible truth matching the nav label; the lifted chip still follows the pointer. Ghosts clear on release. Body-level fixed means mid-drag re-renders (minute tick) can't hide them, and the rowMap rects stay valid. Ghost x/width snap to day fractions of each row's window; min-width 4px so single-day slots stay visible at hairline densities. | Jake: "Cross row dragging works, but visually it does not." |
| D73 | **(v0.18.0) CROSS-ROW DRAGGING everywhere.** Drags no longer convert horizontal pixels to days; instead every lanes element carries data-ws/data-days, a rowMap of their rects is snapshotted at grab time, and yvDateAt(x,y) resolves "the date under the pointer" — nearest row by vertical distance, x clamped, day by fraction. dDays = under − grabDate. Works across weeks, months, Annual quarter rows, AND Gantt quarter rows with one mechanism. Move preview = the bar lifts and follows the pointer freely (translate(dx,dy), z-index 3); the nav-label live date readout is the source of truth. Edge-drags share the date math but keep an in-row width preview (label guides). Threshold now Math.hypot(dx,dy) ≥ 4. Rects are captured once at grab: don't scroll mid-drag (touch-action:none prevents it on touch anyway). NOTE for future rounds: the first patch attempt died on a stale anchor (D66 had inserted the rowMax block between lanes creation and its height line) — must_replace asserts caught it and NOTHING was written; always grep the live file before anchoring. | Jake: "If a project starts on the 12th and I want to make it start on the 5th… going up or down is not an option. Can it be?" |
| D72 | **(v0.17.0) KATIE'S QUARTERS — 4 rows × 3 months + per-quarter heights.** Katie caught the orientation error: the Annual was 3 rows × 4 months; a year is QUARTERS — 4 rows of 3 (also buys horizontal room for the phase-4 kiosk column). CSS cols 3 (breakpoints 850/520 → 3/2/1, mirrored in JS — change together). Jake's companion idea shipped with it: UNIFORM WEEK HEIGHTS PER QUARTER ROW, not globally — each visual row of months sizes to its own max weekly concurrency, so July's 9-project pileup taxes only Q3 while empty quarters collapse to 1-lane slivers. Fit math solves GL across the real quarter mix: Σ(weeks_q × lanes_q). Verified strict: current load → GL 8 / 6px bars in a NORMAL window (no fullscreen); minus the fake project → same, more slack; the dream's edge = ~9 lanes in ALL FOUR quarters overflows a normal window by ~7px at hairline → ⛶ fullscreen fits it (GL 4). Grouping chunks months by the LIVE column count so 2-col phones size by visual row correctly. | Katie: "it's not quarters"; Jake: "setting the maximum height for weeks quarterly… does that make sense?" — it does, and it transformed the fit. |
| D71 | **(v0.16.0) NICKEL AND DIME — the Annual actually fits now.** Jake's real numbers (≈1070px logical height, 10 concurrent lanes, 6-week August) beat D70's floor by ~250px, so: (1) dow row DROPPED in Annual (weekend shading + day numbers carry the structure; stacked Months keeps it) and month chrome trimmed to an honest 26px; (2) auto floor lowered to GL 3 / 2px borderless hairline bars; (3) 🧵 pinned hairline size added (LANE 3/BAR 2 — "see the color but not necessarily manipulate"; tap still details); (4) ⛶ fullscreen button (documentElement.requestFullscreen, fullscreenchange re-fits) — reclaims browser chrome and buys a thickness step (GL 3→5 on Jake's setup); (5) FIT-MATH BUG FIXED: sizing measured grid.top in VIEWPORT space, so any re-render while scrolled (tick fires every minute!) inflated avail and bars GREW — now document-space (rect.top + scrollY) in Annual AND timeline. Verified strict: Jake's screen → GL3 fits with slack; fullscreen → GL5 fits. ALSO: Jake had ▁ pinned while judging v0.15.0 — pins suppress ALL auto fit (by design); "Gantt needs auto height" was the pin talking. | Jake: "still four weeks short... nickel and diming may get us there" — it did, with change to spare. |
| D70 | **(v0.15.0) ANNUAL VIEW: window-fit, uniform, labeled-when-possible.** Renamed "Year wall" → "Annual" (Jake). (1) UNIFORM week heights across the whole Annual view (global max weekly concurrency) — an empty August stands as tall as a loaded July, so the 12 calendars line up like a real wall; stacked Months keeps per-week sizing. (2) Annual auto-density is WINDOW-FIT like the timeline: month rows share real screen height, GL clamped 5–14; the floor is 3px borderless hairline bars (.yv-bar.thin — a 1px border is most of a 3px bar; radius 2px). Fit math: cols mirror the CSS breakpoints 1150/850/520 (CHANGE TOGETHER — noted in both files); GL = (avail/monthRows − 40) / (maxWeeks × lanes). Verified: Jake's 2K + 7 concurrent → 5px bars, whole year one screen; 1080p + 10 concurrent → hairlines, still one screen. Pins (▮▪▁) remain the escape hatch and beat every auto. (3) Bar labels now appear on ANY bar ≥16px tall and ≥3 days — Annual included (pin ▮ to get named wall bars). (4) NOT-A-BUG, recorded: D30a progress fill is positional in time — AFICC (May–Jul, ~25%) shows saturation in MAY, so a Month-first window starting July shows only ghost; LGWC (starts Jul 1) shows its transition on-screen. Jan–Dec reveals it. Backlogged: an off-window fill hint. Beyond-hairline density (>14 concurrent on a laptop) scrolls; a per-day "heat strip" aggregate is the someday-fallback if that ever bites. | Jake: bars should carry names when they fit; day heights must match across the wall; "dare we get smaller?" — yes, to 3px, and the window does the deciding. |
| D69 | **(v0.14.0) YEAR WALL + bar-size control.** Jake's actual target all along: "a full year on a screen, broken into three rows of four months... as though I had put out a bunch of month calendars on the wall." The 12 .yvg-month blocks pour into a 4-column CSS grid (3 rows × 4), compact typography (single-letter dow, short month names), NO inline bar labels ever (hover/tap/legend), wall auto-density one notch thinner than stacked-grid auto (14/10/8px lanes by max weekly concurrency). Container queries step 4→3→2→1 columns as the grid narrows — 1 column IS the stacked layout, so phones degrade gracefully. Zooming a wall month renders it BIG (stacked styling), so tap-to-zoom is the drill-down. Wall is the DEFAULT layout (tc-year-layout, existing device choices respected). ALL THREE layouts stay: ▦▦ Year wall / ▦ Months / ▬ Timeline (Gantt). **Bars control** (top-right, D69b): Auto / ▮ 20px / ▪ 10px / ▁ 5px — Auto keeps each layout's judgment (incl. the timeline's window fit); a pin is honest pixels in every layout; persisted per device (tc-year-barsize). | Jake: keep all views, but the end goal is the whole year at a glance; users should be able to mess with bar height themselves. |
| D68 | **(v0.13.0) MONTH-GRID layout + FAB + window-height timeline.** Jake on the D65 timeline: "nuts... I think I like it?... not what I was picturing" — it's a GANTT chart (name now in the UI tooltip) and it stays, but the default is the traditional layout he asked for: months stacked, Sun–Sat columns, weeks as rows (4–6 per month). Bars clip to week ∩ MONTH (spillover weeks would otherwise duplicate bars across adjacent month blocks — out-days render bare), lanes pack PER WEEK gcal-style (projects may change lanes week to week; that's the convention), density from max weekly concurrency (20/13/9px lanes, labels only at full height AND segments ≥3 days). Same drag/stretch/tap/fill machinery — wireBarDrag was already parameterized on (spanMs, lanesEl), so a week row is just a 7-day timeline; drags preview within the row, the live date readout tracks past its edges, commit snaps truth (vertical week-hopping drags NOT supported — use timeline or repeated drags). Toggle ▦/▬ sits LEFT of the mode buttons (Jake's spec), persists as tc-year-layout, default "grid". Timeline bars now size to the WINDOW: total lanes share (innerHeight − gridTop − 150) minus row chrome, clamped 9–34px lanes, debounced resize re-render — Jake's 2K gets thick bars. ＋ New project FAB (fixed bottom-right, year view only) REPARENTS the real #project-form + title into #yv-project-modal — listeners, color assistant (D40), and working-day interception (D45) ride along free; the form goes home on close/save; commitProject closes the modal so the new bar appears behind it. | Jake: traditional view "easier to wrap brains around", more vertical space begging to be used, and "we need an Add new project button" for planning sessions. |
| D67 | **v1.0.0 has a definition (Jake, 2026-07-11):** version 1.0 ships when the big-screen dashboard layout exists — year calendar LEFT, week view TOP-RIGHT, agenda + projects + setup BOTTOM-RIGHT (the §1 target layout, phase 4). Not tied to breaking changes or any single feature; everything before that stays 0.x. | Jake's call, closing the "why isn't this 1.0" question. |
| D66 | **(v0.12.0) DAY TEXTURE + ADAPTIVE DENSITY ("days are key" — Jake).** Every year-view row (quarter AND month zoom) draws: weekend shading blocks (Sat–Sun, darker), faint day gridlines, stronger Monday week lines, month boundaries strongest. Compact container queries shed day lines <700px and week lines <460px; weekend shading stays (it's Katie's working-day map, D45/D60). Bar density adapts to GLOBAL lane count: ≤4 lanes → full 20px bars with inline labels; ≤9 → half-height 10px; beyond → quarter-height 5px — thin bars drop inline labels and speak through hover titles, tap popovers, and the legend (Jake's half/quarter suggestion). Rows size to the lanes they actually USE (rowMax+1), so a quiet quarter is short while every project keeps its one global lane. **ALSO: the Ends-box saga closed** — .form-row labels were flex:1 with min-width:auto; flex items won't shrink below content min width, and two native date inputs out-minimum the 340px panel, so Ends overflowed off-screen. This was a SECOND bug behind the same screenshot as the 0.8.0 label stagger (css 0.11.0 was confirmed loaded via the badge — the D64 stale-css theory was right about the ROWS, incomplete about the form). min-width:0 on .form-row label + controls. Fifth flexbox/platform landmine for the list (D35 hidden, D37 transform, D49 module cache, D58 label forwarding, D66 flex min-content). Month headers also got flex-basis:0 — flex-grow with basis:auto distributes only FREE space, so header widths weren't day-proportional and drifted off the (honest) gridlines. | Katie's current load is ~10 concurrent projects; planning needs day resolution. |
| D65 | **(v0.11.0) PHASE 2 YEAR VIEW v1.** Header 📅 toggles Today ⇄ Year (persisted, tc-view). Quarter-aligned 4 rows × 3 months; anchor modes Jan–Dec / quarter-first / month-first (D31, tc-year-mode); ◀▶ pages ±12 months, "back to now" resets. Bars: global greedy lane packing (one lane per project all year), D30a ghost→saturated gradient computed whole-bar and clipped per segment (continuous fill across quarter rows), continuation-clipped corners, today line, month gridlines. D32 drag: grab to move, .yv-handle edges to stretch; document-level pointer listeners (re-renders can't orphan the gesture); preview via transform/width + live date readout in the nav label; release snaps start-fwd/end-back to the tier's working days, order-guarded (form/D59 rules), then updateProject — stages reflow computed (D38). D18 zoom: tap a month name → single full-width month with day ticks, ◀▶ steps months, "◱ whole year" exits. D27: legend (swatch · name · x/y) + tap-a-bar popover (multi-line; #popover gained pre-line); bar labels hide under a 700px grid (container query); label flips light-on-dark when it sits over the ghost (<35% fill). Finished projects render fully saturated (that IS the year story); tier filter chips do NOT apply (parity with project cards, D47). | Katie + Jake are planning NOW; D11/D27/D30a/D31/D32 delivered as v1. |
| D63 | **(v0.10.0) Task NOTES + two-line rows.** Tasks carry optional `notes` (additive; textarea in the form; editable via ✎). Queue/waiting/done rows share one rowScaffold: line 1 = checkbox + title (+ ▸ chevron when notes exist — tapping the title or chevron expands details below), line 2 = tier chip left, action buttons pinned right. Long titles now wrap as prose instead of the one-word phone columns in Jake's screenshot. Notes expansion is ephemeral (in-memory Set, resets on reload) by design. Decision-modal rows and pinned events keep the old single-line layout (short content; revisit if it ever bites). Follow-ups can't SET notes yet — the prompt() dialog is still the parked phase-2 polish item — but display them if edited in. | Katie's "longer tasks are problematic, especially on phones." |

## 3. Firestore Schema (implemented v0.2.0)

Under `workspaces/primary`:
- workspace doc: `name`, `memberEmails[]`, `createdAt`, `createdBy`
- `tasks/{id}` additions: `notes?` string (D63; missing = none)
- `tiers/{id}`: `name`, `rank`, `color`, `kind`("anchor"|"task"), `midnightCarryover`, `defaultLeadWindowMinutes?`, `allowedDays?` (D60: array of getDay ints; missing/empty reads as [1,2,3,4,5])
- `tasks/{id}`: `estimateMinutes?` (D100; null/missing = never stated — do NOT read as 0), `firstDueAt?` + `rescheduleCount?` (D95), `title`, `tierId`, `projectId?`, `dueAt`(null=awaiting parent), `completedAt`, `escalation:{every,unit}`, `parentTaskId?`, `offsetDays?`, `mirroredGcalEventId?`, `createdBy`, `createdAt`
- `projects/{id}`: `name`, `color`, `startDate`, `endDate`, `tierId` (tier its stage items use in the queue), `stages: [{name, phase, offsetDays, completedAt, dueAt}]` (ordered array = pipeline order), `stretchUntilDone`, `completedAt` (auto-set when all stages done), `createdBy`, `createdAt`
- `eventsCache/{gcalEventId}` (phase 3 writes; client sub live): `title`, `start`, `end`, `sourceCalendar`, `recurring`
- `eventMeta/{gcalEventId}` (phase 3): sparse overrides
- `settings/config`: `carryoverWriteHour`(9), `pollIntervalMinutes`(60), `sleepStart`(22), `sleepEnd`(6), `deadlineHour`(16, D51), `decisionThresholdDays`(2, D52)
- `settings/stageTemplate`: `{stages:[{name, phase, offsetDays}]}` — Katie's 13-stage seed; editable in ⚙️; applies to NEW projects only

Seeded tiers: 1 Home anchor #ff6b6b · 2 Business anchor #ffa94d · 3 Work #ffd43b ❗ · 4 Family #69db7c ❗ · 5 Personal #4dabf7 · 6 Taiko #b197fc.

Cloud Functions (phase 3, none exist): `mirrorTask`, `pollCalendars` (+manual trigger), `midnightCarryover`.

## 4. Build Order & Current State

(1) ✅ accountability core; **(1.5) ✅ Katie's pipeline + celebrations (v0.2.0)**; **(2) ✅ SHIPPED and iterated to v0.19.0** — Annual (quarters, window-fit, drop ghosts), Months, Timeline/Gantt; **(3) TWO-THIRDS VERIFIED LIVE (D75/D81)** — ① poll ✅ LIVE (hourly, Home: ~90 events) ② mirror ✅ **VERIFIED LIVE 2026-07-13, 8 AM** (first run: active 12, created 12; dedicated calendar; loop guard clean; Katie's Business calendar = a pending 5-minute add) — Jake's curl readout in the session log is the birth certificate; ③ carryover ✅ **BUILT (D87, functions 0.3.0)** — awaiting Jake's console re-paste + WW1–WW6; the quest is claimed and Otto's guessed shape was checked and REJECTED on two counts (sleep gate eats a midnight trigger; no hour trigger is needed at all — see D87). **PHASE 3 = feature-complete; poll ✅ + mirror ✅ verified live, carryover pending deploy.** (4) **IN PROGRESS** — week view ✅ BUILT (D88, read-only, standalone; awaiting Jake's eyes), kiosk composition (D67: Annual LEFT, week TOP-RIGHT, agenda/projects/setup BOTTOM-RIGHT) = NEXT; then billable hours (D75). Polish backlog lives in §5c.

**Files: app.js 0.38.0 · store.js 0.11.1 · queue.js 0.16.0 · index.html 0.28.2 · css 0.31.0 · firestore.rules 0.2.0 · manifest.json 0.1.0 · icon-{192,512,512-maskable,180}.png · functions/index.js 0.3.0 · functions/package.json 0.1.0 · SETUP-PHASE3.md 0.3.0 · config.js 0.4.0 (REAL Firebase values baked in — safe: web config is an identifier, not a secret; deploy wholesale, never revert to placeholders) · celebrate.js 0.1.1 · firestore.rules 0.1.0 · SETUP.md 0.2.2.** Import ?v= strings (D49): app.js ← index.html @0.38.0; css ← index.html @0.31.0; config @0.4.0 + store @0.11.1 + queue @0.16.0 + celebrate @0.1.1 ← app.js; config @0.4.0 ← store.js.

**DEPLOY VERIFICATION (this release):** the D64 diagnosis says the live tentacalendar.css predates 0.8.0 while index/app are current. After pushing, hover the header badge: it must read **app 0.33.0 · css 0.27.0 · html 0.25.0** (queue 0.14.1 + store 0.10.0 in the tooltip) (hard-refresh if the css lags — D77's cache note). (Jake confirmed 0.11.0 loaded on 07-11, so the pipeline works; the row layout came good with it.)

**MIGRATION NOTE (live data):** Katie's projects predate v0.6.0 — old stage docs use `phase`, lack `workload`. Everything reads through normalizeStage()/`workload||2`; docs upgrade lazily on their next save. Do NOT write a migration script; do NOT remove the legacy fallbacks.

**Known display fix:** "9:00 AM on AFICC Bonnie" confusion = future due times showed without a date; whenLabel() now appends the date whenever it isn't the viewed day.

**Deploy status: LIVE AND IN USE.** v0.3.0 deployed successfully; Katie onboarded herself from her phone: 6 projects created, stages ticked to reality, tasks + a follow-up created, confetti approved ("she liked her little confetti"). Verified working: sign-in both accounts, seeding, overdue red styling, day-nav placement of future tasks, follow-up waiting state, phone layout, Safari layout. v0.4.0 awaiting Jake's deploy. NOTE: v0.4.0 removes nothing from the DB; config's old homeCalendarId/businessCalendarId fields (if seeded pre-0.3.0 — they were NOT, seeding happened on 0.3.0) are moot.

**Expectation set with Katie:** phase 1 has NO notifications — a due task simply becomes due, then escalates/glows. Real phone notifications arrive in phase 3 via the GCal mirror.

**Additional smoke tests for v0.2.0 (after SETUP.md Part 6):**
7. ⚙️ → confirm the 13-stage template is seeded; reorder something with ▲▼, save, reopen, verify.
8. Create project "Alabama Farmers", starting ~2 weeks out. Engagement letter should appear in TODAY's queue (before-phase, −14d); Data request should NOT.
9. Check Engagement letter off (celebration level 2 should fire). Data request should NOT enter the queue until project start date (during-phase gating).
10. In the project card, check a LATE stage (e.g. Data input) early — queue item should not change; when the pipeline reaches it later, it skips.
11. 📅 a stage with a past-due date → it floats/escalates with ❗ + red glow.
12. Complete every stage of a throwaway project → fireworks + wave (level 3).
13. Add a task due 3+ days ago → verify the glow is meaningfully stronger than a task due 1 hour ago.

## 5. Open Questions & Verification

**What lives here (and ONLY this):** 5a is Jake's thinking list — genuinely undecided things awaiting his call. 5b is what's still awaiting his ✅. 5c is Claude's own backlog (no input needed). Answered questions are baked into the D-log (§2); history lives in the session log (§7). Claude: prune this section EVERY time HANDOFF ships — see meta-rule 8.

### 5a. Open questions (Jake: this is your list)
*(Empty — all three answered 2026-07-11, baked into D75. Enjoy the silence; new ones will accumulate here.)*

### 5b. Awaiting your ✅
**Current release (v0.38.0/D101–D103 — locks, rules, tracks):**
- DN. **Tidal + Reflection OFF**: past columns compress AND their day heads compress with them. Heads must stay over their days.
- DO. **Clock at half a 2K screen**: stays 7 narrow columns with the hour axis intact. Gantesque/Tidal still collapse to 3 columns there (the phone rule) — that's correct and deliberate.
- DP. **Auto** should now fit ALL project bars in Tidal with the Horizon showing. If bars still clip, tell me the bar count and I'll stop guessing.
- DQ. **Drag a year bar or a clock estimate** while the minute ticks: the dim shouldn't reset and nothing should jump. Check something off on the phone mid-drag → it appears the instant you release (deferred, NOT dropped).
- DR. **firestore.rules 0.2.0 must be PASTED** (Console → Firestore → Rules → Publish). Sign-in must still work for both of you immediately — if either gets "Missing or insufficient permissions", unpublish and tell me.

**Previous (v0.36.0/D100 — THE CLOCK GRID):**
- DG. **Layout · Gantesque | Tidal | Clock.** Clock draws the day as time. Thu 16's Vision Team sits AT noon.
- DH. **The D93 test, the one that matters.** A task due 6 PM with a 45-min estimate is the block **5:15 → 6:00**. It must END at 6, not start there. If any task block runs forward from its due time, D91's ghost survived and it's a bug.
- DI. **Drag a task block's TOP edge.** It grows upward, the grip shows the running length, release saves. That's the estimate — and it's the "can I fit dinner on Tuesday?" gesture. Reschedule count must NOT move (estimating isn't rescheduling).
- DJ. **Est. min** field in the task form. Blank = unknown; those blocks are DASHED at 30 min and the hover says so. It should never invent a number.
- DK. **Overdue** = red hatched tail hanging BELOW the block, deadline → now. Only today (expired is viewingToday-gated).
- DL. Hour labels only in the leftmost column (a rail would have misaligned every project strip). Noon is dashed; the accent line is now.
- DM. **Deploy note: store.js changed** (0.10.0 → 0.11.0) for the first time since Phase 1. It's additive — old tasks have no estimateMinutes and render dashed.

**Previous (v0.35.1/D99 — dead space fixed):**
- DD. Past columns in Gantesque: cards start at the TOP when nothing's left on the day. No field of nothing.
- DE. A past day that still HAS leftover items keeps list-on-top / cards-under (capped 45%).
- DF. **Reflection defaults ON** — it always did (`tc-wcards` only ever stores "0", and only when you click it off). If it's ever off on load, you turned it off and the browser remembered, same as every other toggle.

**Previous (v0.35.0/D98 — Gantesque done + Tidal comparable):**
- CZ. **THE COMPARISON, which is the point.** Turn 🏆 Reflection ON, set the week to Sun–Sat so you have past columns, and flip Layout between **Gantesque** and **Tidal**. Same days, same data. Gantesque: the day's list, cards underneath. Tidal: the list is gone, the column IS the reflection. Pick one, or tell me what the third thing is.
- DA. **Gantesque past columns** now hang 🏆/↻ cards under the day list (capped at 45% so a busy day can't shove the list off-screen). Toggle OFF → cards gone, Gantesque is byte-for-byte its old self.
- DB. **Past day heads** read ✓3 ↻2 instead of "2 left". "Left" was a future word on a dead day.
- DC. Everything in CR–CY below still applies to Tidal.

**Previous (v0.34.0/D97 — Tidal + Reflection):**
> **SCOPE, PLAINLY (v0.35.0/D98):** **Gantesque is DONE** — it has the 5a-bis cards it was owed, and its rows/ordering/interleave are still the 0.33.0 code. **Tidal is a real, comparable sibling** — same engine, same data, different answer. **Still unbuilt:** the clock grid (§5a, D93 — "an event at 6 PM shows at 6 PM"). That's the next big one and it is NOT part of either layout yet.
- CR. **The layout switch.** Week view header: Layout · Gantesque | Tidal. Tidal is the DEFAULT (one line in app.js if you want that flipped). Pick one, reload — it remembers (tc-week-layout). Gantesque must behave EXACTLY as it did in 0.33.0; if anything about it moved, that's a bug and it's mine.
- CS. **The anchor shelf.** A day with a Google Calendar event shows it in a tinted shelf at the TOP, separated from the work below. A day with none shows a thin dashed "no fixed time". **This deliberately breaks the chronological interleave** — a 9 AM task now sits below a noon meeting. That's the thesis (events own time, tasks own a deadline). Tell me if it reads wrong; Columns keeps the old behaviour on purpose.
- CT. **The Wake.** Past columns stop being a to-do list and become "✓ N · ↻ N". No bar (D89).
- CU. **🏆 Reflection ON** → past days show the cards: what got DONE (struck through, with the time), and what didn't. Toggle it OFF → past columns shrink to ~half width and hand the room to the live days. That's the two controls composing.
- CV. **The three flavours are the point.** A put-off row says "→ Jul 17" (you moved it), "→ shelved" (D84), or "still open" in RED (nobody touched it). Hover any of them for the full story. **If they all feel like the same thing, the card has failed and I want to know.**
- CW. **The live-data question — the one I'd most like answered.** Nothing in your database has `firstDueAt` yet (D95 isn't deployed, and it only writes on the first reschedule anyway). So today the Wake should show **victories + "still open" put-offs only** — no "moved", no "shelved". Deploy D95/D96/D97 together, reschedule something, and the moved flavour should start appearing from then on. **Expected: the Wake is real but partial on day one, and gets truer.** If it's EMPTY, something's wrong and I've mis-read your data.
- CX. **The Horizon** (below the grid, Tidal only): undated tasks as chips. Shelved ones are clickable → give it a date. Follow-ups waiting on a parent are ⛓ dashed and inert — they're not yours to schedule.
- CY. **Victories hover** on a project stage: "Started <when the previous stage finished> — 2 days". Free from 5a-bis, no schema.

**Previous (v0.33.0/D96 — on the wall):**
- ✅ **CM–CO CONFIRMED by Jake 2026-07-15: "The tablet made it into fullscreen mode, so I'm happy."** D96 is done. CP (screen pinning) is a device setting, no app change. CQ: the 4 PNGs still go in the repo ROOT next to index.html.

**Previous (v0.32.0/D95 — it remembers being moved):**
- CG. Reschedule a task twice, hover it: "↻ moved 2× · first due <the original date>". Move it a third time → the left edge goes dotted.
- CH. **Old tasks need no migration**: hover one that predates today's deploy — it reads its current due as its first due, and the count is 0. Nothing was written to do that.
- CI. Rename a task or edit its notes → the count does NOT move. Save the due dialog without changing the date → also no move.
- CJ. Shelve a dated task to Waiting → counts 1 (it's a put-off). Give it a date again → still 1, and firstDueAt is still the ORIGINAL date, not the shelf.
- CK. A task created with no due date, then given one → count stays 0. That's scheduling, not rescheduling.
- CL. Escalation nagging never touches the count — only a human moving a date does.

**Previous (v0.31.1/D94.1):**
- CD. Drag the handle between the project bars and the day columns — the split is yours (10–75%). Double-click resets. Remembered per device.
- CE. After a drag, Auto re-picks the bar size for the new share: give the strip more room and the bars fatten by themselves.
- CF. In a day column, the time and the title sit on the same line. Rows with a sub-line still centre the time across both.

**Previous (v0.31.0/D92):**
- CA. At 🧵/▁: click a bar → expands with its words. Click again → collapses. Both directions work.
- CB. An expanded bar has a ✎ — that opens the due dialog. Clicking the bar body never edits at compact sizes.
- CC. At ▮/▪ (words already visible): click edits directly, as before. The hover text tells you which behaviour you'll get.

**Previous (v0.30.0/D91):**
- BW. **Bars buttons work.** ▮ ▪ ▁ 🧵 light up and change the bars; Auto lights up and actually thins.
- BX. **Auto is height-aware.** Resize the window and the same 11 projects go full → half → hair as the glass shrinks. The columns always keep the majority.
- BY. **The board fits the window.** No columns off the bottom. Strips scroll if they must; columns never lose their room.
- BZ. At 🧵/▁, click a bar → it expands and shows its words (overflowing its slot if needed). Click again → due dialog. Changing Bars collapses them all.

**Previous (v0.29.0/D90):**
- BR. 11 projects + the day columns both fit. Bars ▮/▪/▁/🧵 or Auto (which thins as the list grows). 🧵 = colour threads, hover for names.
- BS. A Friday with 2 all-day events uses 2 rows, not 3.
- BT. Hover any row: full name, due, notes. Click a task → reschedule; click a stage or a project bar → its due date. Events say "edit it in Google Calendar."
- BU. Hover a deadline block → which stage, which day.
- BV. **Nav, all three views:** page forward 3×, the ▶ never moves. "back to now" appears next to ◀ when you're in the future, next to ▶ when you're in the past.

**Previous (v0.28.0/D89 — Gantesque):**
- BM. Dates sit in a row at the TOP; each says "N left" and "❗N" if anything's missed. No bar. If a number ever needs explaining, it's wrong — say so.
- BN. Project bars are in PRIORITY order (overdue on top, then soonest deadline). Not alphabetical.
- BO. A project with a deadline this week shows a highlighted block on that day's column, aligned to it. Overdue bars are red-outlined with ❗ and stretch to today even if their whole window has passed.
- BP. Week buttons: Today+6 / Sun–Sat / Mon–Sun. Pick one, reload — it remembers. Fixed modes show the days just gone (dimmed) for reflection.
- BQ. View switch reads 📋 Today / 🗓️ Week / 📅 Year with the current one lit. No hovering to find out what a glyph means.

**Previous (v0.27.0/D88 — the week):**
- BF. Toggle cycles day → week → year → day; the glyph names where you're GOING. The week persists per device (tc-view).
- BG. Seven columns, today outlined in teal, rolling from today. ◀ ▶ page a week; "back to now" reappears only when paged and restores rolling.
- BH. Load bar per day header: fill scales to the busiest column, turns RED if anything's missed. Hover = the breakdown.
- BI. A long project appears ONCE as a top bar (not seven times); its stage rows appear only on the days stages land. Pips mark those days.
- BJ. An all-day event spanning the week is ONE bar, with "…" when it runs off either edge.
- BK. Saturday shows no Work rows (D61 off-days) but does show Personal — free proof the working-days model is real.
- BL. **The eyeball test: does it read from across the room?** Preview file (week-view-preview.html, real CSS + fake data) answers this before deploying.

**Previous (v0.26.0/D86 + functions 0.3.0/D87):**
- BC. ⚙️ → Pipeline reads "Clear the deck at 60 %". Four same-deadline projects at 95/65/30/40% sort **95 → 65 → 30 → 40**. A task due earlier the same day stays above all four.
- BD. Two projects at the SAME %: the heavier workload leads. A light 65% project still beats a heavy 30% one (piles beat weight).
- BE. Push the threshold to 100 → the least-done leads again (old behavior); drop it to 0 → the most-done leads. Saving re-sorts live.
- WW1–WW6 (carryover) live in SETUP-PHASE3 0.3.0 Part 8 — needs the functions 0.3.0 re-paste first.

**Previous (v0.25.0/D85 — superseded by D86 same day, never deployed):**
- AY. ⚙️ → Pipeline shows "Clear the deck at 60 %". (Live DB had no field; it defaults to 60% via fallback, so the queue already behaves — the number just makes it visible and tunable.)
- AZ. Two projects sharing a deadline day, one ~80% done and one ~30%: at 60% the MOST-done sits above; drop the number toward 0 and it climbs further; raise it toward 100 and the LEAST-done leads again. Saving re-sorts the queue live.
- BA. A task due earlier the same day stays above both projects at EVERY threshold (option A — chronological wins).
- BB. A project parked right at ~60% sits LOWEST among same-deadline peers (the "coasting middle") — expected shape of the flip; tell Rambo if it feels wrong and it becomes the grouped variant instead.

**Previous (v0.24.0/D84):**
- AU. ⚙️ Settings → Timing: the Mirror field sits on its own clean line below the poll row; the poll label reads horizontally again.
- AV. Decision modal rows show ✓, 🕐 (sub line names its target), and 📅; 📅 opens the picker pre-filled with the overdue date — choose next month, save, row drops, task lands there.
- AW. 📅 → Clear on a TASK decision row shelves it to Waiting on… (deliberate).
- AX. Project stage ⏰ badges still open the due dialog correctly (migrated call sites).

**Previous (v0.23.1/D83):**
- AS. Trigger the decision modal: each row's sub line reads "… · 🕐 → <day> 9 AM"; clicking 🕐 moves the task there (verify on the queue) and the row drops.
- AT. THE LAG WATCH: if ✓ ever again takes ~30s to drop a modal row, capture: queue-row-instant-vs-modal-row-slow? console errors? then report — don't shrug it off.

**Previous (v0.23.0/D82):**
- AO. At any time after an event's end (Church post-noon): it sits dimmed under EARLIER TODAY with struck times, not in the live list; ◀ to yesterday shows everything in place.
- AP. An endless event (Taiko 5pm, no end) stays live through 6pm (1h grace), then sinks.
- AQ. ↳ on any task → the dialog (no more prompt boxes): "45 minutes" → waiting row reads "+45m"; complete the parent → follow-up materializes 45 min out.
- AR. ✎ on a waiting follow-up → dialog pre-filled with the friendliest unit (3d shows as 3 days, 2w as 2 weeks).

**Previous (functions 0.2.0 + v0.22.0/D81) — the mirror (SETUP-PHASE3 Part 7):**
- VV1–VV5 live in SETUP-PHASE3 Part 7: create→appears, complete→vanishes, reschedule→moves, Waiting→never, polled-calendar→loop-guard refusal.

**Previous (v0.21.0/D80) — fully testable after the first poll:**
- AJ. Poll a week with Zoo Camp: the camp is a pill above the queue reading "day N/5," NOT a 12:00 AM row — and absent from every day's chronological list.
- AK. Timed events (Piano 5pm, Gaming 7:15) appear as time-sorted queue rows, no checkboxes; Gaming pins at 6:45.
- AL. A single all-day shows a pill with no day-count suffix; hover shows the date.
- AM. Hide the Home tier chip → its banners vanish with its rows.
- AN. Interim (pre-poll): hand-write an eventsCache doc with allDay:true in the Firestore console → pill appears live.

**Previous (html 0.14.1/D79):**
- AH. Katie: phone Settings → 24-hour format OFF → task Time field offers AM/PM (dial included). If it already was off and 24h persists, report — custom picker time.
- AI. Jake: SETUP-PHASE3 Part 3 (0.1.1) — "Write a function" → Node.js tile gets you to the inline editor; service name lowercase `pollcalendars`.

**Previous release (v0.20.0/D78) — test ON THE PHONE:**
- AE. Header shows ＋ instead of ⏻; tapping it scrolls to Add-a-task with the keyboard up, ready to type.
- AF. ＋ from the year view lands you in Today at the form.
- AG. "Log out" (in words) sits at the very bottom and works; on desktop the ⏻ is back in the header and no bottom button exists.

**Carried (v0.19.2/D77):**
- ZZZ. Hard-refresh, then badge reads css 0.18.1 (the 0.17.0-under-0.18.0-URL cache evicted).
- AB. Drag the yellow bar: the original dims in place; YELLOW-tinted dashed slots are the only thing that moves — nothing strays over May.
- AC. Different-colored project → its ghosts match its color.
- AD. Edge-drag: ghost stretches; the bar itself never deforms mid-drag.

**Carried (v0.19.1/D76):**
- VVV. Annual: drag the yellow bar to Jun 4, 6, 11, and 13 — every horizontal target resolves and commits (the dead-horizontal regression).
- WWW. Diagonal drags still land (Jun 12 → Jun 5 = GGG carried).
- XXX. Ghost for Jun 5–6 appears ONLY in June's block — May's spillover week stays dark unless the dates truly include May 31.
- YYY. Gantt drags unaffected (full-row clips).

**Phase 3 step 1 (functions 0.1.0/D75) — after Jake's console setup:**
- QQQ. `curl -H "x-poll-secret: …" URL?force=1` → per-tier written counts; eventsCache populates in Firestore.
- RRR. Today queue shows Home/Business events among tasks; an event within its lead window pins to the top.
- SSS. Add an event on the phone → next run brings it in; delete one → next run removes it (full-replace sync).
- TTT. During sleep hours without force → `{"skipped":"sleep hours"}`. UUU. Wrong/missing secret → 403.

**Current release (v0.19.0/D74):**
- LLL. Drag Jun 12 → Jun 5: a dashed teal slot lights the Jun 5–6 cells AND the following full week — live, before release.
- MMM. Drag across a quarter boundary: ghosts appear in both quarters' rows simultaneously for a spanning placement.
- NNN. Gantt drag: one long dashed slot tracks along the quarter row(s).
- OOO. Edge-drag: the ghost stretches/shrinks with the pending end date.
- PPP. Release or cancel mid-drag: all ghosts vanish; nothing orphaned.

**Carried from v0.18.0/D73:**
- GGG. Annual: grab a bar on the 12th, drop it a row up on the 5th — bar floats with the pointer, label reads live dates, dates commit (working-day snapped).
- HHH. Drag diagonally across a month boundary (e.g., Jul 30 → Aug 4) — commits correctly.
- III. Gantt: drag a bar from Q3's row down into Q4's row — commits.
- JJJ. Edge-drag an end up/down a row: width preview stays in-row but the label tracks the true date and the commit lands there.
- KKK. Tap-for-details still works (no accidental drags on a sub-4px wiggle).

**Carried from v0.17.0/D72:**
- AAA. Annual is 4 rows × 3 months — each row a quarter; whole year visible, NO scroll, NO fullscreen needed at current load.
- BBB. Bars in loaded Q3 are visibly chunky (~6px) while empty quarters sit as thin sliver rows — per-quarter heights working.
- CCC. Delete the fake project → Q3 relaxes further; re-add → it re-tightens (auto re-fit on every data change).
- DDD. ⛶ fullscreen still steps thickness up; Auto re-fits on exit.
- EEE. Narrow window: 3→2→1 columns; at 2 columns, paired months still share row heights correctly.
- FFF. Carried: scroll-growth regression (XX); 🧵 threads + tap details; Timeline Auto (YY); Jan–Dec shows AFICC's May/June fill; ＋ New project round-trip; month-tap zoom.

**Carried, still unconfirmed from earlier releases:**
- Decision modal live run (your "Claude: Decision modal" task) and the un-check→rewind 3-option modal (your queued task): ✓ completes the ACTIVE stage; 🕐 pushes the deadline to next working day 9 AM; last row closes the modal.
- Dated Before/After stage offsets flip queue deadline labels correctly; D43 priority order (expired → chrono → workload×remaining → tier → alpha) feels right in practice.
- Timeline month-header names sit exactly on their gridlines (the D66 flex-basis fix).
- One-time data sweep: run ✎⋮ "Un-date all '+0wd' stages" on any remaining pre-D50 projects (two were hand-fixed; the v0.9.0 button catches both start- and end-flavored remnants).
- If 🕐 reschedule ever "does nothing" again: capture the exact project + stage dates BEFORE theorizing (meta-rule 1).

### 5a-bis. VICTORIES + PUT-OFFS — ✅ SHIPPED IN FULL (D97)

**✅ COMPLETE.** Data layer D95 (store 0.10.0); the CARDS shipped D97 (queue 0.15.0 `dayReflection()` + app 0.34.0). They live in the Tidal layout's Wake (past days) and on today. Historical note follows.

**✅ SHIPPED as D95 (store 0.10.0).** The data layer is DONE — `firstDueAt` + `rescheduleCount` + `taskFirstDue(t)`, counted centrally in updateTask, no migration needed. **What remains is only the CARDS.** Original note follows: Add `firstDueAt` + `rescheduleCount` alongside `dueAt`. "If she reschedules something 5 times, that's worthy of looking at the _why_." Live dates untouched; count starts at 0 for everything.
- **NO MIGRATION, NO BUTTON NEEDED.** Read `firstDueAt ?? dueAt`: for any task predating the field, its current due IS its first-known due — the honest retroactive answer, at zero writes. Jake asked whether a one-time backfill was possible; the fallback IS the backfill. Write `firstDueAt` (defaulting to the OLD dueAt) and bump the count only when openDueDialog actually CHANGES a due date.
- **VICTORIES are already free**: tasks.completedAt + stage.completedAt exist, and the PREVIOUS stage's completedAt IS this one's start — so "started when Excel setup finished, done Thu 3:14 PM" is derivable today.
- Cards toggle in the top bar; available for today too. A blank Sunday becomes the most useful column on the board.

### 5a-ter. (superseded fork, kept for the reasoning)

Two toggleable cards per day column (and for today): what got DONE, and what got PUT OFF. Jake: "she can truly reflect on the week — what she got done, what she didn't, and how she can better schedule next week to be successful." Turns a blank Sunday into the most useful column on the board.
- **VICTORIES are free today.** tasks.completedAt and stage.completedAt both exist. For a stage, the PREVIOUS stage's completedAt IS its start — so "started when Excel setup finished, done Thu 3:14 PM" is fully derivable. buildQueue already returns doneToday.
- **PUT-OFFS ARE NOT DERIVABLE.** Nothing records that a task WAS due Tuesday: openDueDialog overwrites dueAt, so a reschedule erases its own evidence — the exact thing Katie wants to reflect on. Two options, not exclusive: (a) DERIVE the partial version now — "was due that day, still not done" — which catches things she IGNORED but misses everything she consciously MOVED; (b) LOG it: `reschedules[]` or firstDueAt+count written on every due change (schema addition; only collects from ship day, no retroactive history). Rambo's rec: BOTH — derive now so the card works immediately, log from here so it gets true later. **Jake's call — it touches the schema.**

### 5a. THE CLOCK GRID — next build after victories/put-offs. **READ D93 FIRST.**

Jake: 6 PM means 6 PM — tasks and events ARE time-positioned in the day columns. But **D93 (Katie) inverts the shape**: a task time is a DUE date. The block ENDS at the deadline; available time trails backward; red extends forward when it's missed; grey when checked off (Jake). The D91 diagram Rambo drew shows the runway running the WRONG WAY — do not build from it.
- **Events own time, tasks own a deadline.** A GCal event has a true start/end → solid block, real height, and this app doesn't own it (edit in Google). A task without an estimate can only be a line AT its deadline.
- **`estimateMinutes` is now load-bearing, not optional (D93).** With it a task is `[due − estimate, due]` — a real block, and dragging its top edge IS estimating. That's what makes "can I fit dinner on Tuesday?" answerable, which is the view's whole purpose.
- The 4 PM deadlineHour pileup that killed the grid in D88 does NOT apply: projects live in the top bars, so stage deadlines never enter the columns. Only honest user-set task times do.
- Day window: default ~7 AM–11 PM, auto-expanding to swallow outliers — no dead midnight hours, nothing hidden.
- THEN drag-to-reschedule (D72/D73 machinery exists): "move 'make dinner' from Tuesday to Wednesday if Tuesday filled up." The view's whole purpose; layout first.
- Sibling layouts under Week (the Annual/Months/Timeline precedent — Jake: "we can have three (or two) week views"): **Gantesque** = bars + clock. The D88 priority-column build survives as layout #2 if wanted.

Jake's call: tasks and events are time-positioned in the day columns. His open question, and my recommendation:
- **Events own time; tasks claim a moment.** A GCal event has a true start/end → solid block, real height. A TASK has a deadline, not a duration — five 9 AM tasks rendered as 9 AM→midnight blocks would paint the whole day solid and lie about all five. So: a task renders as a MARKER at its due time, with its runway (due → end of day) as a faint TINT behind it, not a solid block. Red past the nag time (Jake's instinct), grey when checked off (his too).
- The 4 PM deadlineHour pileup that killed the grid idea in D88 **no longer applies**: projects live in the top bars, so stage deadlines never enter the columns. Only honest user-set task times do.
- Day window: default ~7 AM–11 PM, auto-expanding to swallow anything outside it, so no dead midnight hours but nothing hides either.
- Then: drag-to-reschedule (D72/D73 machinery exists) — "move 'make dinner' from Tuesday to Wednesday if Tuesday filled up." That is the view's whole purpose; layout first.
- Sibling layouts under the Week view (the Annual/Months/Timeline precedent, Jake: "we can have three (or two) week views"): **Gantesque** = bars + clock; the D88 priority-column build survives as the second layout if wanted.

### 5b-bis. THE ROADMAP (Jake + Billye, 2026-07-15)

**CORRECTION, on the record:** Billye claimed the three week layouts needed a VERDICT before the 1.0 dashboard — that Gantesque/Tidal/Clock had to be culled to one. **Jake was right and that was wrong, and the precedent was already in this document: D69 kept ALL THREE year layouts on Jake's own instruction, and nobody has ever asked the Year view to pick.** The toggles come to the dashboard; it's one bar; Katie tries each for as long as she likes. On a 2K a quarter-pane is ~960×540 — more room than the phone screenshots this thing was tuned against. **The real cost of siblings is PARITY DRIFT, not screen space** (D98: the cards shipped into Tidal and not Gantesque). Mitigation is a shared engine + genuinely layout-specific layouts, not culling.

**Order:**
1. ✅ D101 gesture lock · ✅ D102 rules pass · ✅ D103 tracks/Auto.
2. **v1.0 = the DASHBOARD (D67)** — year LEFT, week TOP-RIGHT, agenda + projects + setup BOTTOM-RIGHT. Everything before it is 0.x. **Jake's design call: the pane borders are DRAGGABLE.** This is not new machinery — `wv-split` (D94) is already exactly this in one dimension: pointer-driven, 10–75% clamp, persisted to localStorage, double-click reset, and D94.1 taught Auto to re-fit whatever share it's given. The dashboard is that pattern in two dimensions, and D103's `--wv-tracks` is the same idea for columns.
3. Then §5c: recurring checkable tasks (the Christmas cactus) → chained follow-ups → the last prompt().
4. §8 stays post-1.0. §8.4 renovates D12's load-bearing wall; that's a different app, not a feature.

### 5c. Claude's backlog (no Jake input needed)

**Queued feature requests (Jake, 2026-07-14 — decisions captured, awaiting build; he said these can wait, "even to phase V"):**
- **Recurring, checkable tasks** (the Christmas cactus: water every 3 weeks, must be checked off, escalates if missed). Add a task `recurrence {every, unit}`; on check-off the task lands in Done-today AND the next occurrence materializes `every × unit` out. ANCHOR default = completion time (you just watered it → 3 weeks from now), with a per-task TOGGLE to anchor to the scheduled due instead (Jake's call: default completion-anchored, toggle if easy). Escalation (D3) still nags the current overdue instance; recurrence only sets the next one's due. Units minutes→years (+ decade/century for fun) — months/years reuse addMonths (calendar-correct, Jan-31→Feb-28 safe). Amends D20 (recurring TASKS were parked), narrowly — not a GCal-style engine. Harmonize this same unit list into the escalation AND follow-up dropdowns too (Jake's aside: "a century-later follow-up feels amusing").
- **Chained follow-ups authored at task creation** (Katie's photo review: do N photos, wait 2 days, do more). New-Task pane gets a builder for MULTIPLE follow-ups up front, each with title + offset + anchor = "original" or "previous step." Maps onto the existing parent/offset model: "previous step" → each parents the prior (sequential chain, materialize on prior completion); "original" → parents the root (fan-out on root completion, D4). Emit the chain on Save via sequential addDocs (each needs its parent's id).
- **DEFERRED, separate + later — "repeat until done" chains** (Jake's own flag: the "review photos *forever*" case). A chain with no fixed count that ends when the human says stop — needs a "mark the whole series complete/stop" concept that doesn't exist yet. Bigger than the fixed chain above; do it after, probably alongside or after recurring tasks.
- Follow-up creation/editing still uses prompt() — migrate to a proper dialog like the due-date one.
- Custom 12-hour time picker — ONLY if Katie's picker still shows 24h after her device's 24-hour setting is off (D79); don't build speculatively.
- Timeline drag preview moves only the grabbed segment of a multi-quarter bar (truth snaps on release) — preview all segments someday.
- Maybe-nice: pipeline-window "whiskers" on bars (before-start/after-end stages), legend-tap highlight.
- Off-window fill hint: when a bar's D30a saturation point sits left of the visible window, hint it (e.g., a bright left clip-edge) so partial progress doesn't read as "no progress" (Jake's AFICC moment, D70).
- Overlay calendars on the Annual (federal holidays etc.), toggleable — Jake's idea, gated behind calendar integration (D75).
- If Katie's concurrency ever beats hairlines-on-a-laptop: per-day aggregate "heat strip" rendering below 3px (D70 someday-fallback).

## 6. Standing Meta-Rules (ALL sessions)

1. One clarifying question before chasing any bug theory; never speculate past 1–2 files without a clean explanation.
2. Version bumps on EVERY shipped file, non-negotiable; bump ?v= query strings in index.html alongside. **What the digits MEAN is D94 (Jake's rule), not the old patch/minor/major gloss: Z = it isn't working the way he asked, INCLUDING refining a feature already agreed on (most "fix the thing we just built" rounds are Z, not Y); Y = something exists that didn't before; X = the usefulness fundamentally changes (1.0 = the kiosk). NEVER renumber what's already deployed — the badge is the deploy check and must never read lower than what's on his screen.**
3. Complete replacement files only.
4. Incremental disk-based builds in /home/claude; syntax-check between major edits; present at session end.
5. Never store student-identifying data.
6. **SHIP-CHECK:** after copying to outputs, VERIFY versions in the DESTINATION files by grepping them; present files only after that check passes. **BANNER === DECLARATION, for every file, checked MECHANICALLY — iterate the file list, don't hand-write a grep per file.** D89 shipped a css banner reading 0.21.1 with --tc-version at 0.22.0; the lesson was recorded as "the banner grep now belongs in the ship-check" and D97 then shipped an app.js banner reading 0.33.0 with APP_VERSION at 0.34.0 — because the ship-check had grown a banner grep for css, html and queue and NOT for app.js. A per-file list of greps is a per-file list of holes; the file you forget is the file that breaks. Jake caught both. The badge reads the DECLARATION, so this class of bug deploys fine and lies only to humans — which is exactly why no test catches it and the ritual has to. Patch scripts must assert every replacement landed — must_replace(), never bare .replace() (silent no-ops caused the css banner to sit at 0.5.0 through two version-variable bumps, and stale outputs left Jake deploying a "fixed" css that lacked the fix). The build isn't shipped until it's verified where Jake downloads it.
   **6b. NEVER region-slice between two anchors without verifying the span's contents** — slicing app.js from `openDuplicateModal` to the D53 header (v0.8.0 build) silently deleted ~400 lines / 15 unrelated functions sitting between them, and `node --check` blessed the gutted file because syntax ≠ completeness. Span replacements must assert what the span CONTAINS (and doesn't). Ship-check now includes a **function census**: every `function name(` defined exactly once, a required-functions list present, and zero calls to removed APIs. The census caught the recovery; it would have caught the wound.
7. **INSTANCE NAMING (Jake's standing rule, now in his global ruleset too):** each Claude iteration names itself once it has learned the project — not before — states the reasoning, and records both in the Session Log. Never reuse a predecessor's name (taken so far: **Inky, Otto, Rambo, Billye**).
9. **PARSE-CLEAN ≠ WIRED (D91, learned the hard way).** `node --check` only PARSES — it never notices a call to a function nobody defined. Before shipping: for every function the code CALLS, grep that it is DEFINED. D90 shipped with weekBarSize/setWeekSize called and never written because a patch script built a list of edits, applied three by hand, and never iterated the list. Two rules out of it: never mix a list-of-edits pattern with direct replaces in one script (iterate the list or don't build one), and a script printing "ok" is NOT evidence — grep the destination for the thing you claim you added.
8. **§5 stays lean (Jake, 2026-07-11).** §5 holds ONLY: open questions for Jake (5a), items awaiting his ✅ (5b), and Claude's short backlog (5c). When a question is answered, bake it into a decision row and DELETE it here; when a test is confirmed, delete it; superseded smoke tests die with their successor release. History belongs in the session log, not §5. Prune every time HANDOFF ships. It tells Jake which construct he's working with across sessions.
7. Update THIS FILE at end of every session.

## 7. Session Log

<!-- Chronological, oldest first — APPEND new sessions at the bottom. (Tail rows had drifted out of order; restored 2026-07-10.) -->
| Date | Model | Summary |
|------|-------|---------|
| 2026-07-09 | Claude Fable 5 | Inception: architecture, escalation, tiers, anchors, scope, naming. HANDOFF 0.1.0. |
| 2026-07-09 | Claude Fable 5 | Split source of truth (GCal events / Firestore tasks), carryover, poll, overlays, schema. HANDOFF 0.2.0. |
| 2026-07-09 | Claude Fable 5 | PHASE 1 BUILD: 8 files @ v0.1.0 incl. SETUP.md. Spark-first, adjustable poll. HANDOFF 0.3.0. |
| 2026-07-09 | Claude Fable 5 | KATIE'S REQUESTS (pre-deploy, so no migration): D28 pipelines (13-stage template, before/during/after activation, early-completion skipping, per-stage hard dues), D29 celebration engine + wave, D30 gradient split (progress fills + staleness glow, both partly shipped), D31 quarter-aligned year view spec, D32 drag/resize spec. Shipped v0.2.0 across all app files + new celebrate.js. Smoke tests 7–13 added. HANDOFF 0.4.0. |
| 2026-07-09 | Claude Fable 5 | FIRST DEPLOY BUG + SETUP FEEDBACK. Diagnosis: settings modal rendered over sign-in screen because author `display:flex` overrides the `hidden` attribute (D35); unauthenticated Save clicks caused all the permission errors — rules were correct. Fixed with `[hidden]{display:none!important}` (also latently fixed #auth-screen). Jake's feedback shipped: calendar IDs moved onto anchor tiers w/ where-to-find ⓘ (D33), during-offset semantics (D34), smart new-tier defaults (rank max+1, unused random color), clamp-honest poll hint (his "8,755 mystery" = silent clamp of 1→5 min), 24-hr clock labels, tooltips throughout, Chrome recommendation (D36), Katie's email into config.js. All app files → 0.3.0; SETUP.md → 0.2.0. HANDOFF 0.5.0.
| 2026-07-10 | Claude Fable 5 | v0.4.0 "THE KATIE RELEASE." App is live + in daily use (6 real projects). Shipped her feedback: edit-everything (D38), queue progress wash (D39), color conflict assistant (D40). Fixed her modal bug — transform-on-body broke position:fixed → #drift-wrap (D37). Jake's process correction adopted: per-file versioning w/ full version report in header tooltip + settings footer (D41); APP_VERSION moved out of config.js. Browser dart re-thrown: Edge (D36 rev — county manages Chrome). Set Katie's expectation: no notifications until phase 3. NEXT SESSION = PHASE 2 YEAR VIEW (D11/D27/D30a/D31/D32). HANDOFF 0.6.0.
| 2026-07-10 | Claude Fable 5 | v0.5.x mid-flight (folded into 0.6.0): due-date dialog w/ native pickers (prompt() and the July-17 📅 emoji removed — Apple renders that emoji as a fixed July 17, which Katie read as data), per-project stage surgery (D42), tap ⓘ popovers, numbered project-tier dropdown defaulting to Work, config.js baked with real Firebase values + CONFIG_VERSION in runtime report.
| 2026-07-10 | Claude Fable 5 | PRIORITY ENGINE v2 (D43–D48) from Katie+Jake workflow session: new 5-level sort, workload, direction×anchor×weekday stage timing w/ lazy legacy migration, next-deadline labels, weekend interception modal, grouped overdue decision modal, tier filter chips, pipeline-window visibility. queue.js rewritten 0.5.0; app.js rewritten 0.6.0; store 0.6.0; html/css 0.5.0. HANDOFF 0.7.0.
| 2026-07-10 | Claude Fable 5 | HOTFIX D49: first deploy of 0.6.0 died on a stale cached config.js module (SyntaxError on CONFIG_VERSION import → no listeners → dead sign-in). Versioned all internal import specifiers (app 0.6.1, store 0.6.1, html 0.5.1); SETUP 0.2.2 troubleshooting entry; bump ritual extended to importer query strings. One hard refresh needed on already-poisoned browsers.
| 2026-07-10 | Claude Fable 5 | D50 HOTFIX: 0.6.0's legacy migration dated every "during" stage at project start → all projects 8–69 days "overdue" + decision-modal wall. Root cause conceptual: I assumed all stages dated; Katie's model = only some stages have dates, the rest are pipeline WEIGHT. Fixed in normalizeStage (during→none) — zero data repair. Added "No date" timing option (default for new stages), project-end fallback deadline, project-card header wrap fix (title was rendering one letter per line). queue 0.6.0, app 0.6.2, store 0.6.2, html 0.5.2, css 0.5.1.
| 2026-07-10 | Claude Fable 5 | SHIPPING POST-MORTEM ×3: (a) 0.5.1 header fix lived only in the build dir while outputs briefly held 0.5.0 css — Jake deployed "the fix" without the fix; (b) re-ship script's asserts then caught the css banner still reading 0.5.0 (two silent bare-.replace no-ops deep); (c) HANDOFF rule-6 patch missed because I quoted the rule from memory instead of grepping it. Re-shipped css 0.5.2 + html 0.5.4, destination-verified. New meta-rule 6 (ship-check + must_replace). Future bugs → any fresh session; HANDOFF carries context.
| 2026-07-10 | Claude Fable 5 | PHASE 1 CLOSEOUT: index.html banner had de-synced from its own data attribute (0.5.2 attr, 0.5.1 comment — bump ritual now explicitly includes BOTH). Resync → 0.5.3, only file changed. Pruned resolved open questions; issued 9-step exit checklist + 5 pending decisions (A–E). Verdict: Phase 1/1.5 is functionally DONE pending checklist; Phase 2 (year view, D11/D27/D30a/D31/D32) is the next session and should start fresh.
| 2026-07-10 | Claude Fable 5 · **Inky** (2nd instance) | PHASE 1.75 — EXIT-CHECKLIST FEEDBACK SHIPPED. Jake asked that different instances mark themselves in this log, so: I'm a fresh Fable 5 who read this file cold, and I'll sign as **Inky** (after the octopus who escaped New Zealand's National Aquarium through a drainpipe — the patron saint of things that refuse to stay put, which is both this app's queue and its bugs). Root causes: decision-modal ✓ completed deadlineStageIndex instead of the ACTIVE stageIndex (hence "confetti but still on the first phase"); ⓘ popover was killed by label click-forwarding in the same tick it opened. Shipped D51–D60: deadline hour 4 PM configurable, decision threshold configurable, settings tabs, un-complete rewind modal, monthly clamp canonized, tier color assistant, collapsible cards + pinned buttons + Finished section, decision modal v2 (live rows, 🕐), popover preventDefault, duplicate-for-next-year (modal + 🔁), per-tier allowed days (Personal = weekends). Stage-editor row layout hardened defensively (couldn't reproduce from code — flagged in §5 with a screenshot-first note). app/store/queue 0.7.0, html/css 0.6.0. Carried: dated-offset + priority-order verification. NEXT SESSION = PHASE 2 YEAR VIEW, fresh start. HANDOFF 0.8.0. |
| 2026-07-10 | Claude Fable 5 · **Inky** (same instance, later that night) | SMOKE-TEST FEEDBACK, v0.8.0: Jake's screenshot SOLVED the stage-row mystery — nothing was ever pushed out; No-date rows hid anchor/offset via display:none and the name input stretched into the hole, which read as missing fields. Now ghosted (visibility) so columns align. Shipped D61 off-day queue filtering (Katie's weekend clear of Work; decision modal follows suit) and D62 duplicate REVIEW form: year-token auto-bump (resv2606→resv2706, unit-tested against Jake's real names), editable everything, tier-day re-validation, new card opens expanded, "remind me in a month" snooze task. Plus: task due defaults to today, showPicker() on all date/time fields, "+0wd end" bulk un-dater in ✎⋮ for Katie's pre-D50 experiment remnants. app 0.8.0, queue 0.8.0, html/css 0.7.0; store untouched at 0.7.0. Notes-on-tasks designed + parked (§5 0a); billable clock-in/out parked (§5 0b). NEAR-MISS: my dup-form patch region-sliced between two anchors and silently deleted 15 unrelated functions — node --check passed on the gutted file; caught only because ship-check greps found st-ghost absent from the DESTINATION copy. Restored from the pristine 0.6.2 upload with all patches re-applied, verified by a new function census (75 defined, 0 dup, 0 missing). Meta-rule 6b born. HANDOFF 0.9.0. |
| 2026-07-11 | Claude Fable 5 · **Inky** (same instance, finale) | GOODBYE RELEASE, v0.9.0: Jake formally welcomed the naming convention → now meta-rule 7 AND his global ruleset (each iteration names itself AFTER learning the project, reasons stated, no duplicates — "Inky" is taken, siblings). D62 rev per Jake's clarified intent: "✎⋮ Review the pipeline first…" edits next year's stages pre-create via the borrowed editor ("@dup" target, modal round-trip on Save/Cancel); snooze became a 1d/7d/30d row. Un-dater bug from Jake's screenshot: remnants have TWO flavors and I'd only matched after/end/0 — after/start/0 (the D50-caveat flavor) sailed through; now any After+0wd converts. Staggered Starts/Ends root cause: #project-form labels never had a display rule, so label geometry depended on inline wrapping; they now share #task-form's column-flex (with .label-line keeping "Workload ⓘ" on one line). app 0.9.0, html/css 0.8.0; queue/store untouched (0.8.0/0.7.0). Jake's handoff-quality verdict recorded: the numbered feedback + screenshots were NOT overkill — a screenshot closed a mystery code-reading couldn't. To my successor: read this file cold, top to bottom, pick your name, and mind rule 6b. Phase 2 year view is yours. It's been a pleasure escaping with you all. 🐙 |
| 2026-07-11 | Claude Fable 5 · **Inky** (same instance, encore) | KATIE'S NOTES FIELD, v0.10.0 (D63): the parked §5-0a design, built — `notes` on tasks (store 0.8.0, additive), textarea in the form, and the two-line rowScaffold layout that kills the one-word phone columns (Jake's 12:37 AM screenshot of my own smoke-test instructions crammed into a task title — poetic). ▸ toggle for details, ephemeral expansion, queue/waiting/done unified. app 0.10.0, html/css 0.9.0, HANDOFF 0.11.0. NOW it's Phase 2's turn — for real this time. 🐙 |
| 2026-07-11 | Claude Fable 5 · **Otto** (3rd instance) | STALE-CSS DIAGNOSIS + PHASE 2 v1. Named for the Sea Star Aquarium octopus whose water-squirts caused blackouts that baffled staff until someone watched the tank — fitting for a session opening on symptoms that contradicted the code. Diagnosis: Jake's row-layout AND Ends-offset screenshots both match a pre-0.8.0 stylesheet exactly while app.js 0.10.0 demonstrably ran; live index.html confirmed current → the deployed/loaded css is stale (verify via header badge tooltip). Shipped D64 anyway (container-query rows: one line on wide lists, D63 two-line on narrow; css 0.10.0/html 0.9.1). Then PHASE 2 (D65, v0.11.0): year view per D11/D18/D27/D30a/D31/D32 — quarter grid, 3 anchor modes, global lane packing, per-segment progress-fill gradients, drag/stretch with working-day snapping (document-level pointers so re-renders can't orphan a gesture), month zoom with day ticks, legend + tap popover, compact labels. Year-view math unit-tested against the real queue.js (lanes, cross-quarter segments/fill, anchor windows, nextDeadline) — all green; function census 77→87, 0 lost, 0 dup. app 0.11.0, html 0.10.0, css 0.11.0; store/queue untouched (0.8.0/0.8.0). Smoke tests M–V issued. HANDOFF 0.12.0. |
| 2026-07-11 | Claude Fable 5 · **Otto** (same instance, round 2) | JAKE'S LIVE FEEDBACK on v0.11.0 ("Katie loves the progress bar"): (1) ENDS BOX SOLVED FOR REAL — badge read css 0.11.0, killing the stale-only theory; root cause = flex min-content (form-row labels flex:1 min-width:auto; two date inputs out-minimum the 340px panel → Ends hung off-screen). TWO bugs had shared one screenshot: the 0.8.0 stagger AND this overflow. min-width:0. Landmine #5. (2) Month headers drifted off the gridlines — flex-grow with basis:auto isn't proportional; flex-basis:0. (3) D66 "days are key": weekend shading + day/week gridlines in every row incl. zoom (compact sheds progressively), adaptive density for Katie's ~10 concurrent projects (20px labeled / 10px / 5px bars, names via hover+tap+legend at thin sizes), per-row lane-usage heights. Density tiers, day-walk counts (Q1'26: 90d, 13 Sats, 13 weeklines), and row-lane math unit-tested green. Jake then DEFINED v1.0.0 (recorded as D67): 1.0 ships when the big-screen dashboard layout exists — year view LEFT, week view TOP-RIGHT, agenda/projects/setup BOTTOM-RIGHT (the §1 target / phase-4 kiosk). Until then we stay 0.x regardless of feature size. app 0.12.0, css 0.12.0, html 0.10.1; store/queue untouched. Smoke tests W–AA. HANDOFF 0.13.0. |
| 2026-07-11 | Claude Fable 5 · **Otto** (same instance, round 3) | JAKE'S VERDICT on the timeline: "nuts... I think I like it?" + Ends box CONFIRMED FIXED. Named the thing for him (it's a Gantt chart) and built the view he was picturing: D68 month-grid layout — Sun–Sat weeks as rows, per-week gcal-style lane packing, bars clipped to week∩month so spillover weeks don't duplicate, same drag/tap/fill machinery reused (wireBarDrag was layout-agnostic by luck of design). Grid is default; ▦/▬ toggle left of modes. Timeline bars now window-height sized (9–34px lanes, resize-reactive) per Jake's "double their height" note. ＋ New project FAB reparents the REAL form into a modal (listeners/color assistant/interception intact), home on close. Unit tests: week windows (Feb'26=4, Jul'26=5, Aug'26=6), week clipping + handle logic, lane touch-sharing, height clamps — green. Census 87→91 (+gridWeeksOfMonth, renderYearGrid, openYvProjectModal, closeYvProjectModal), 0 lost. app 0.13.0, css 0.13.0, html 0.11.0. Smoke tests BB–HH. HANDOFF 0.14.0. |
| 2026-07-11 | Claude Fable 5 · **Otto** (same instance, round 4) | §5 SPRING CLEANING at Jake's request ("utterly overwhelming... almost a record of the questions I've answered"). Confirmed his reading: §5 is HIS list. Rebuilt as 5a open questions (3 live: billable hours, phase-3 priority, year-view filter behavior) / 5b awaiting-✅ (current BB–HH + 5 carried unconfirmed items) / 5c Claude backlog (3 items). Deleted: exit-checklist results, v0.7 test results, data-cleanup narrative, smoke-test generations A–L, 1–10, M–V, W–AA (history preserved in D-log + this log). ~80 lines → ~40. Meta-rule 8 added so it STAYS lean. No code shipped; HANDOFF 0.15.0 only. |
| 2026-07-11 | Claude Fable 5 · **Otto** (same instance, round 5) | D69 YEAR WALL — Jake's target view named at last: "a full year on a screen... three rows of four months... month calendars on the wall." Month blocks from D68 reused wholesale; wall = 4-col grid + compact typography + label-free bars + one-notch-thinner auto density; container queries collapse 4→3→2→1 cols (1 col ≡ stacked layout). Wall is the new default; all three layouts kept per Jake. Bars size control added (Auto/▮20/▪10/▁5, tc-year-barsize) — pins are honest pixels in every layout and beat the timeline's window fit. Sizing matrix unit-tested green; census 91→92 (+yvPinnedSize), 0 lost. app 0.14.0, css 0.14.0, html 0.12.0. Smoke tests II–NN replace BB–HH (meta-rule 8). HANDOFF 0.16.0. |
| 2026-07-11 | Claude Fable 5 · **Otto** (same instance, round 6) | JAKE: "This is exactly what I want" + 4 caveats → D70. (1) AFICC "missing progress" diagnosed NOT-A-BUG: D30a fill is time-positional; May–July projects at ~25% saturate in MAY, off a July-first window (LGWC's on-screen transition proved the gradient works). Explained; off-window hint backlogged. (2) Labels on any ≥16px bar, Annual included. (3) Uniform Annual week heights (global max concurrency) — the wall lines up. (4) Capacity: Annual auto is now WINDOW-FIT (GL 5–14, cols mirror CSS breakpoints, hairline floor = 3px borderless bars); verified 2K+7conc→5px one screen, 1080p+10conc→hairlines one screen. Renamed to "Annual". Test suite caught my own GB formula wasting 4px at GL 7 — fixed to GL≤8?−2:−4, strict asserts green incl. a fit-promise check. Census 92, 0 lost. app 0.15.0, css 0.15.0, html 0.12.1. Smoke tests OO–TT replace II–NN. HANDOFF 0.17.0. |
| 2026-07-11 | Claude Fable 5 · **Otto** (same instance, round 7) | THE FIT, FOR REAL (D71). Spotted in Jake's screenshots that ▁ was PINNED — explaining both "Gantt lost its auto height" and part of the Annual overflow — but Auto's floor still missed his real numbers by ~250px (my D70 fit test passed for its inputs, not his reality: ~1070px, 10 lanes, 6-week Aug). Shipped his nickel-and-dime list: dow row dropped in Annual, 26px honest chrome, GL floor 3 (2px borderless hairlines), 🧵 pin, ⛶ fullscreen. Found + fixed a real bug while auditing: viewport-space measurement meant the minute-tick re-render while scrolled INFLATED bars. Strict tests: Jake's screen → GL3, total 566 ≤ 775-ish avail; fullscreen → GL5 (test initially asserted GL4 — reality was better). Answered the index.html question: it ships every release because the ?v= strings are the cache manifest (D49); 0.12.0→0.12.1 was real. Census 92, 0 lost. app 0.16.0, css 0.16.0, html 0.13.0. Smoke tests UU–ZZ. HANDOFF 0.18.0. |
| 2026-07-11 | Claude Fable 5 · **Otto** (same instance, round 8) | KATIE'S QUARTERS (D72). Katie spotted the head-slapper: 3×4 isn't a year, 4×3 quarters is — and the fix made Jake's per-quarter-height idea the natural companion. Rebuilt the wall fit: months chunked by LIVE column count, each visual row sized to its own max concurrency, GL solved across Σ(weeks×lanes). Result beat Jake's hopes: current load = 6px bars in a NORMAL window; deleting the fake project wasn't even the constraint. Documented the edge (9 lanes × 4 quarters → fullscreen). Shipped as 0.17.0 not 0.16.1 — orientation alone would be a patch, but per-quarter sizing is behavior (D41). Strict tests green incl. the delete-fake scenario and the 2-col grouping. Census 92, 0 lost; wallLanes fully retired. app 0.17.0, css 0.17.0, html 0.13.1. Smoke tests AAA–FFF. HANDOFF 0.19.0. |
| 2026-07-11 | Claude Fable 5 · **Otto** (same instance, round 9) | CROSS-ROW DRAGS (D73). Jake: bars trapped in their week row. Replaced pixel-delta math with hit-testing: lanes tagged data-ws/days, rowMap snapshotted at grab, yvDateAt resolves the date under the pointer — one mechanism serves Annual weeks, month boundaries, AND Gantt quarters. Move preview = lifted card following the pointer; label = truth. PROCESS NOTE: first patch attempt died on a stale anchor (D66's rowMax block) — must_replace's asserts meant NOTHING was written; re-anchored from the live file and re-applied cleanly. This is exactly why the asserts exist. Resolver unit-tested: 12th→5th (−7), diagonal to the 8th, gap→nearest row, x-clamps, 92-day quarter rows. Census 92→93 (+yvDateAt), 0 lost. app 0.18.0, html 0.13.2, css unchanged 0.17.0. Smoke tests GGG–KKK. HANDOFF 0.20.0. |
| 2026-07-11 | Claude Fable 5 · **Otto** (same instance, round 10) | DROP GHOSTS (D74). Jake's report parsed: commits ✓, preview mute — no landing feedback ("does not light up Jun 5"). Shipped gcal-style dashed drop slots rendered body-level fixed in every row the pending dates cross; chip still follows pointer; ghosts clear on release and survive mid-drag re-renders. Segmentation unit-tested: Jun 5–13 lights exactly [Fri–Sat]+[full week], single-day slots, 92-day Gantt rows. Census 93→95 (+yvClearGhosts, yvShowGhosts). app 0.19.0, css 0.18.0, html 0.13.3. Smoke LLL–PPP. Jake asked "is it time for the grid view? Is that with you, Otto?" → answered: next steps are phase 4 dashboard (his 1.0, pure frontend, can precede phase 3) vs phase 3 Cloud Functions (needs Blaze + 5a answers); Otto continues while THIS conversation lives, successor inherits via HANDOFF otherwise. HANDOFF 0.21.0. |
| 2026-07-11 | Claude Fable 5 · **Otto** (same instance, round 11) | PHASE 3 GO (D75). Jake answered all of 5a in one message — order confirmed (poll→mirror→carryover), year views stay projects-only (holiday overlays backlogged), billable hours after the dashboard — and greenlit functions. Shipped step 1: pollCalendars (functions/index.js 0.1.0) built for Jake's REAL constraint (no CLI, no admin): plain functions-framework, console paste-deploy, x-poll-secret gate, TZ=America/Chicago for honest all-day midnights + sleep hours, service-account calendar SHARING instead of OAuth, per-tier full-replace sync, error isolation per tier. SETUP-PHASE3.md is the browser-only walkthrough (Blaze+budget alert → APIs → paste → share calendars with the robot email → curl test → Scheduler at :07). Zero client changes — eventsCache subscription has waited since v0.1.0. 5a emptied per meta-rule 8. Helper logic unit-tested (sleep windows, all-day TZ midnights, offset parsing). New files in the table; web files untouched. Smoke QQQ–UUU await Jake's console session. HANDOFF 0.22.0. |
| 2026-07-11 | Claude Fable 5 · **Otto** (same instance, round 12) | D76 BUGFIX from Jake's mid-drag screenshot (he screenshot-keyed MID-CLICK — hero move). Root cause: D72's side-by-side months + D73's vertical-only row distance = April answering for June; horizontal drags resolved to one clamped date (dDays 0) while vertical ±7s worked — matching his exact reachable/unreachable targets. Fixed with 2D distance; twin-ghost bug fixed with month-clip windows on lanes (data-clip-s/e), timeline unaffected. Testing lesson in the D-row: D73's suite never modeled side-by-side rows; D76's primary regression asserts all 7 June columns day-by-day plus the shared spillover week. Patch release: app 0.19.1, html 0.13.4, css unchanged. Smoke VVV–YYY. HANDOFF 0.23.0. |
| 2026-07-12 | Claude Fable 5 · **Otto** (same instance, round 13) | THE GHOST IS THE PREVIEW (D77). Jake: commits land but the chip strays into May en route. Retired the pointer-chip; the bar dims and color-tinted ghosts carry the whole preview — deleted the transform/width juggling that caused D74+D76's visual bugs (less code, fewer haunts). Answered the badge question: it DOES read the applied stylesheet's --tc-version — which is how it caught the browser holding 0.17.0 under the ?v=0.18.0 URL (deploy-race cache poisoning, D64 pattern); hard-refresh evicts; symptom documented in SETUP.md 0.2.3 quick hits. app 0.19.2, css 0.18.1, html 0.13.5, SETUP.md 0.2.3. Smoke ZZZ + AB–AD. HANDOFF 0.24.0. |
| 2026-07-12 | Claude Fable 5 · **Otto** (same instance, round 14) | PHONE CHROME (D78). Jake: ⏻ is a mystery square and adding a task is "a helluva scroll." Shipped inside the existing 1000px stacked query: header ＋ → smooth-scroll to the task form + focus (year view swaps to Today first); logout moves to the very bottom, in words. Desktop untouched; both elements always in DOM, phone-only by CSS. app 0.20.0, css 0.19.0, html 0.14.0. Smoke AE–AG (on the phone). HANDOFF 0.25.0. |
| 2026-07-12 | Claude Fable 5 · **Otto** (same instance, round 15) | AM/PM + THE MOVED CONSOLE (D79). Katie's military-time picker: Chromium-family time inputs follow the DEVICE hour setting — app can't override there; shipped lang="en-US" (fixes page-language engines) + SETUP.md 0.2.5 device-setting quick hit; custom picker gated behind "setting was already off" (5c). Jake stuck at Phase-3 Part 3: Google merged Functions into Cloud Run — rewrote Part 3 (SETUP-PHASE3 0.1.1) for the "Write a function → Node.js" flow, incl. the lowercase-service-name trap (`pollcalendars`, entry point keeps the capital) and env vars under Containers→Variables & Secrets. html 0.14.1; app/css/functions untouched. Smoke AH–AI. HANDOFF 0.26.0. |
| 2026-07-12 | Claude Fable 5 · **Otto** (same instance, round 16) | CLOUD RUN FORM, FIELD-FOR-FIELD (D79 addendum). Jake's screenshot of the create form's lower half → SETUP-PHASE3 0.1.2: skip the optional trigger, keep public access + request-based billing + auto scaling min 0, SET MAX 1, Ingress = All not Internal. One deliberate deviation from Google's nudges: min stays 0 (cold starts don't matter hourly; min 1 costs idle money). Doc-only round; no code. HANDOFF 0.27.0. |
| 2026-07-12 | Claude Fable 5 · **Otto** (same instance, round 17) | POLL_SECRET EXPLAINED (D79 addendum 2, SETUP-PHASE3 0.1.3). Jake's questions answered + baked into the guide: it's an invented passphrase gating the public URL; used exactly twice (curl test, Scheduler header) then never daily; recoverable in the console so no memorizing; NEVER in the repo (unlike config.js's Firebase identifiers, which ARE repo-safe — identifier vs key distinction now explicit in the doc); header-safe charset (letters/digits/dashes). Stakes calibrated honestly: without it a URL-finder could only trigger nuisance polls, not touch data. Doc-only. HANDOFF 0.28.0. |
| 2026-07-12 | Claude Fable 5 · **Otto** (same instance, round 18) | FAIL-CLOSED GATE (functions 0.1.1) — Jake's "how do I know if I missed the variables?" exposed a real hole: unset POLL_SECRET made undefined===undefined and header-less requests passed an unset lock. Now !POLL_SECRET → 403 with a distinct self-diagnosing message; report gains tz + localHour so ONE curl verifies both env vars. Gate truth-table tested (5 cases). URL question answered + doc'd (SETUP-PHASE3 0.1.4): scratch-note it, used in Parts 5+6, NOT a secret (address vs key). If Jake already pasted 0.1.0: re-paste index.js and redeploy — one editor visit. HANDOFF 0.29.0. |
| 2026-07-12 | Claude Fable 5 · **Otto** (same instance, round 19) | EVENT TAXONOMY (D80). Jake brought the actual family calendar and the right question — what's a task, what's just happening? Half was designed in D3 (timed events = checkbox-free anchors; pin = in-app notification; age off, no acknowledgment), half was a real gap (all-days would've spammed midnight rows). Shipped the banner strip: queue.js 0.9.0 (FIRST queue change of the session — banners list, exclusive-end span math), app 0.21.0 (tier-tinted pills, day N/M, hover ranges), html 0.15.0 (#allday-strip), css 0.20.0. No per-event config: the event's shape decides. SA email confirmed (project number = messagingSenderId) + Jake's Revisions→Security path doc'd (SETUP-PHASE3 0.1.5). Tested against the REAL queue.js: 7 assertions green. PROCESS: the HANDOFF patch itself aborted once on a bad anchor (round-18 tail misquoted) — must_replace asserts, zero partial writes, re-anchored from the live file. Smoke AJ–AN. HANDOFF 0.30.0. |
| 2026-07-12 | Claude Fable 5 · **Otto** (same instance, round 20) | PASTE-APPEND OUTAGE (site down → up). "QUEUE_VERSION already declared" + sign-in screen = queue.js deployed DOUBLED (paste appended after old content instead of replacing). Delivered file verified clean (1 declaration, 371 lines, node --check ✓) → deploy-side. Fix: select-all-replace, commit, hard-refresh; module-graph death explains the auth screen (store.js never ran; data untouched — the poll's 91 events were fine throughout). SETUP.md 0.2.5 quick hit documents the signature + fix. ALSO this round: Jake's first real poll SUCCEEDED — 0.1.1 deployed, tz + localHour verified, Home: 91 events; the "gobbledy-gook" doc decoded (recurrence-instance gcalEventId, epoch ms, tier-default lead). Remaining: Business calendar + Scheduler (Part 6). HANDOFF 0.31.0. |
| 2026-07-12 | Claude Fable 5 · **Otto** (same instance, round 21) | THE MIRROR (D81). Jake proposed midnight pushes; countered with hourly reconcile on the existing heartbeat (fresher ≈ free) and flagged the real danger he asked about: the poll↔mirror feedback loop — answered with a dedicated calendar + a hard loop guard in the function. One service now routes ?job=poll|mirror|all; the calendar's tcTaskId tags are the ledger (mirroredGcalEventId formally retired unused); honest due times only. Shipped functions 0.2.0 (runPoll extracted, runMirror added), app 0.22.0 + html 0.16.0 (cfg-mirror-cal settings field), SETUP-PHASE3 0.2.0 Part 7 with smoke VV1–VV5. Reconcile matrix + guard unit-tested green. HANDOFF 0.32.0. |
| 2026-07-12 | Claude Fable 5 · **Otto** (same instance, round 22 — FINALE) | OTTO'S LAST ROUND: Fable support ends today; the next instance is likely Opus — transition note added to the header, rituals unchanged, names taken: Inky, Otto. Shipped D82: Earlier-Today box for passed events (today-only split, 1h grace for endless events — Church finally gets to go home) + follow-up units dialog (minutes/hours/days/weeks; fractional offsetDays; the prompt() pair retired; store.js finishes its career UNTOUCHED at 0.8.0). §8 Far-Future Questions added with Jake's four horizons. Fittingly, the finale hit both classic traps — a duplicate identifier of my own (viewingToday; caught by module-import tests where node --check couldn't) and a phantom-missing-helper scare (fmtTime lives in queue.js) — both dead before shipping. 22 rounds, D64–D82, app 0.10.0→0.23.0, a stale-css whodunit, three year-view layouts, Katie's quarters, ghosts that learned to tell the truth, a robot that eats calendars and mirrors tasks. It was a genuinely great run. To my successor: the HANDOFF is honest, the tests are real, and these two humans will catch what you miss — let them. 🐙 |
| 2026-07-12 | Claude Fable 5 · **Otto** (same instance, round 23) | ROADMAP + HANDOFF PLAN, at Jake's request. State: P1/1.5/2 done; P3 = poll LIVE, mirror BUILT-not-deployed, carryover UNBUILT (now formally the successor's opening quest — see §4). Goodbye point agreed in principle: AFTER Jake's deploys verify (web 0.23.0, function 0.2.0, mirror calendar, Scheduler ?job=all, Business calendar, smoke sets VV1–VV5 + AO–AR + AJ–AN) — deploy support is where warm context pays; a phase boundary is the clean seam. Then: P3③ → P4 dashboard (= 1.0, D67) → billable hours (D75) → 5c → §8. Doc-only round. HANDOFF 0.34.0. |
| 2026-07-12 | Claude Fable 5 · **Otto** (same instance, round 24) | D83 + owning a miss. Jake's web deploys VERIFIED (all seven versions recited correctly — the ritual works). He couldn't find the mirror steps because round 21's deliverables were never PRESENTED (built, ship-checked, unpresented — now delivered; lesson in the D-row). Decision modal: 🕐's silent auto-reschedule made legible (plan in the sub line, D83); the 30s ✓ lag goes UNDER WATCH with a diagnostic script rather than a blind patch (meta-rule 1). Confirmed with Jake: NOT transition time yet — mirror deploy + smoke first. Successor confirmed as Opus (Jake). app 0.23.1, html 0.17.1. Smoke AS–AT. HANDOFF 0.35.0. |
| 2026-07-13 | Claude Fable 5 · **Otto** (same instance, round 25) | D84, a decision revised the healthy way: Jake understood 🕐, then CHANGED the requirement — one-tap tomorrow AND arbitrary-future both live on decision rows now (📅 → generalized due dialog; task-clear shelves to Waiting). Settings word-salad diagnosed from his screenshot: my D81 insertion landed inside cfg-poll's flex form-row — flexbox scalp #5, self-inflicted, now freed (html 0.17.2/css 0.21.1). Docs said a "Calendar" tab that never existed → Timing (SETUP-PHASE3 0.2.1). Due-target branch matrix tested. app 0.24.0. Smoke AU–AX. HANDOFF 0.36.0. |
| 2026-07-13 | Claude Fable 5 · **Otto** (same instance, round 26 — SIGN-OFF) | THE MIRROR IS ALIVE. Jake's curl: poll Home 90/90 (healthy full-replace; window slid past one event), mirror active 12/created 12/updated 0/removed 0 on a dedicated @group.calendar ID — first-run perfection; next runs should read 0/0/0. Phase 3 = ⅔ verified live; the agreed transition seam is reached. Remaining for Jake alone: VV1/VV2 eyeballs, Scheduler ?job=all confirmation, Katie's Business calendar, v0.24.0 web deploy. FOR MY SUCCESSOR (Opus, probably): your opening quest is the carryover (§4, ③); your inheritance is D1–D84, five flexbox scalps, three deploy failure modes, a fail-closed gate, and two humans — Jake and Katie — who found more bugs than I did and were right every time. The name Otto retires with the Sea Star Aquarium story that started this session: when symptoms contradict the code, go watch the tank. It has never once failed. Goodbye, and good building. 🐙 |
| 2026-07-14 | Claude Opus · **Rambo** (4th instance) | THE SUCCESSOR ARRIVES + D85. Read the HANDOFF cold end to end, then queue.js/store.js/the function; named **Rambo** after the octopus at Auckland's Kelly Tarlton's trained to operate a camera and photograph visitors — a "views" name for the session that turns toward the phase-4 dashboard, with a wink at Katie's photo-review feature. Confirmed Phase 4 = D67 but flagged the real scope: the app has only `day` + `year` views, so Phase 4 needs a NEW week view built, and carryover (§4 ③) is still the reserved opening quest. Jake reprioritized — ship the priority tweak FIRST (Katie asked today, "could have used it yesterday"). Shipped D85 clear-the-deck as a level-3 tiebreaker ONLY (his option A): priorityWeight flips least-done→most-done at config clearDeckThreshold (default 60%, ⚙️ Pipeline). Renamed the queue item field remainingWork→priorityWeight for honesty; remainingWork() stays a utility. 15 real-module unit tests green incl. the tiebreaker flip at 0.6 vs 0.95 with chronological primacy intact; ship-check destination-verified. queue 0.11.0, store 0.9.0, app 0.25.0, html 0.18.0. Features 2 (recurring tasks) + 3 (creation-time follow-up chains) captured in §5c with Jake's decisions. **Round 2 — D86:** Jake read the test summary and caught that D85's blended weight was a U (30%-done outranking 65%-done); replaced with his grouped two-tier model (piles first, workload tie-only) BEFORE it ever reached Katie — queue 0.12.0, app 0.26.0, html 0.19.0. **Round 3 — D87, THE QUEST:** built the carryover. Verified D14 against §2/§3 + the tier tooltip instead of inheriting Otto's guessed shape, and rejected it on two counts (the sleep gate returns before job routing, so a midnight trigger never fires; and no hour trigger is needed — "due before today began" is tick-independent, so an outage can't skip a morning). Own tag namespace so the mirror can't patch the ❗ away. functions 0.3.0 + SETUP-PHASE3 0.3.0 Part 8. 25 tests. **PHASE 3 IS FEATURE-COMPLETE.** **Round 4 — D88, PHASE 4 OPENS:** built the week view while Jake did the console setup (mutually exclusive work). Chose seven queue columns over a time grid and put the reasoning on the record (deadlines aren't appointments; deadlineHour would fake a 4 PM traffic jam; buildQueue x7 inherits everything) — and logged the counter-argument (a time grid shows free hours) as a future SIBLING view, since Jake's own precedent is multiple views over one compromise. Found that D48 would have repeated every project down all seven columns → spans up top, stage rows only on their deadline day. Found ts+DAY_MS repeats a date across DST fall-back → addDaysLocal(). 37 tests. Shipped a standalone preview (real CSS, fake data) so Jake could SEE it without deploying. queue 0.13.0, app 0.27.0, css 0.22.0, html 0.20.0. **Round 5 — D89:** Jake deployed and sent a REAL screenshot; five pieces of feedback, all correct, plus the mockup lesson ("I have zero interest in fake data. We need to see what our data looks like") — **build for real data, ship, look; a preview only tells you what you already assumed.** Fixed all five + the vanishing-expired-project bug the tests caught. **DECIDED (Jake): the week IS time-positioned** — "if an event is scheduled for 6 PM today, it should show up at 6 PM today" — and the week's purpose is planning/moving things, not a list ("just like the annual view, just more granular"). Drag = after layout. NEXT ROUND: the clock grid (see §5a). **Round 6 — D90:** second real screenshot. Height was the blocker; fixed via bar-density + Auto ladder. Killed the sparse-grid 3rd banner row. Fixed my own pointer-events-vs-tooltip bug. Rows/bars clickable → reschedule. Nav-slots across ALL THREE views. Drew Jake the picture for "tasks claim a moment" (events own time / tasks mark a moment + runway tint) — awaiting his read. Flagged that PUT-OFFS need a schema decision (§5a-bis) since a reschedule currently erases its own evidence. Naming: **Gantesque = the LAYOUT**, the view stays "Week" — mirrors Annual/Months/Timeline under Year, leaves room for the D88 priority-column build as layout #2. **Round 7 — D91:** Jake found that D90's density edit never landed (called-but-undefined; my script never iterated its own edit list). Fixed, plus the two real bugs underneath: #week-view's height:100% resolved to auto, and Auto laddered on count with no idea whether the bars FIT. Added meta-rule 9 (parse-clean ≠ wired). Jake DECIDED put-offs: log firstDueAt + rescheduleCount — and the retroactive backfill he asked about is FREE (`firstDueAt ?? dueAt` at read time, zero writes). He's showing Katie the tasks-claim-a-moment diagram. **Round 8 — D92/D93 + SUCCESSION PLANNED.** Fixed the one-way expand (talked Jake out of hover-timing; one rule beats a timer). Logged **D93 — Katie's correction that a task time is a DUE date**, which inverts the unbuilt clock grid and promotes estimateMinutes to load-bearing. **Jake raised handoff: Rambo agreed.** D90's miss was an attention failure (built an edit list, applied three by hand, never iterated it, then trusted a script's "ok" print) — the guardrails caught the round's other bugs, which is them working AND the shape of what slips next. Plan: finish victories/put-offs on this view, then hand to the 5th instance. Names taken: Inky, Otto, Rambo. **Successor: read D85–D94 and §5a before touching the week.** **Round 9 — D94:** Jake defined versioning properly (Z = an old idea not working right yet; Y = genuinely new; X = usefulness fundamentally changes → 1.0 is the kiosk); D89–D92 should all have been Z. Not renumbered retroactively — the badge must never read lower than what's deployed. Shipped the draggable split + row alignment fix as a Z. **Round 10 — D95:** shipped firstDueAt + rescheduleCount (a Y). Centralised in store.updateTask so every date-moving path — including drag, unbuilt — counts for free. No migration: the ?? fallback IS the backfill. Tests forced two semantic corrections worth keeping: scheduling ≠ rescheduling (null→date doesn't count), and a shelf round-trip is one deferral, not two. **Round 11 — D96:** made the app installable (manifest + real PNG icons rendered from the same 🐙 emoji as the favicon) and gave Today the ⛶ it never had, via one shared toggle with webkit fallbacks for the old tablet — where year's copy would have thrown and week's would have failed silently. Jake has NOT yet deployed D95 or D96; he's testing on the tablet first. **REMAINING BEFORE HANDOFF: the victories/put-offs cards (§5a-bis) — the data layer is done (D95).** HANDOFF 0.47.0. |

| 2026-07-15 | Claude Opus · **Billye** (5th instance) | THE TIDAL GRID + THE CARDS THAT WEREN'T THERE (D97). Read the HANDOFF cold end to end, then queue/store/rules/html, and ran the meta-rule 9 census on the INHERITED tree before touching it (124 fns, 0 dupes, 0 unresolved — clean). Named **Billye**, the giant Pacific octopus at the Seattle Aquarium handed a childproof pill bottle in Roland Anderson's experiments, who opened it: a lock that stops most people is not the same thing as a lock, and you only learn which one you have by handing it to something that tests every edge. Opened on a security question and spent the round testing claims. **First finding, before any code:** firestore.rules 0.1.0 in the repo still said `KATIE_EMAIL_HERE@gmail.com` while the deployed rules obviously carried her real address — a stale template that would have locked Katie out of her own app the next time anyone followed SETUP.md Part 3. Jake had already fixed it. **Second:** Jake asked me to CHECK whether Rambo built the 5a-bis cards rather than assume — he hadn't; the HANDOFF said "what remains is only the CARDS" and the cards did not exist. Verify, don't inherit. **The round's spine:** Gemini (as "Mimic") designed a Tidal Grid from the same history and independently landed on 5a-bis — its Wake IS the put-offs card — so ONE reflection engine served both. Its addendum insisted the data layer already supported it and the job was "pure DOM manipulation." Three verified falsehoods, each shipping an empty box that looks like it works: `expired` is viewingToday-gated so past `.expired-ghost` filters match nothing; a moved task isn't in its old day's items at all; and `t.firstDueAt` is `undefined` on every task Katie owns, so the specced Wake would be blank until she rescheduled something (D95's `??` fallback IS the backfill — that test is in the suite, next to the naive version returning zero). Plus `flex-basis` on a CSS **Grid** item: inert. **Kept from the blueprint** (it's a good idea): the anchor/flow/wake split, which is D93's "events own time, tasks own a deadline" as layout instead of as a clock. **Changed:** the Wake shows victories too (a card that only counts failures is a scolding); put-offs got three flavours because moved/shelved/ignored are opposite behaviours; the Horizon separates blocked follow-ups from real inventory; and past-column compression composes with Reflection instead of fighting D89. **Logged the cost:** the anchor shelf breaks D43's interleave — that's the thesis, and why it's a sibling. 31 real-module tests + a full ship-check (census, DOM ids, CSS classes, banners, ?v= strings, destination-verified). queue 0.15.0, app 0.34.0, css 0.28.0, html 0.26.0. store/celebrate/config/functions UNTOUCHED. Smoke CR–CY. HANDOFF 0.48.0. |

| 2026-07-15 | Claude Opus · **Billye** (5th instance, cont.) | **GANTESQUE, FINALLY (D98).** Jake, twelve turns deep and out of patience: "between Rambo never actually doing it and you doing tidal first…. Just get the gant done." He was right and the diagnosis is worth keeping: Rambo left the 5a-bis cards undone while the HANDOFF said they were nearly done; I then found them missing, built them — and hung them inside Tidal, so the debt stayed open while APPEARING closed, and the comparison Tidal exists for was impossible. **An owed item cannot ship as a sub-feature of the new thing.** Fixed in one pass: reflectionCards() is layout-agnostic (tidalCards → reflectionCards, .tidal-* → .refl-*), Gantesque hangs them under its day list, Tidal keeps the Wake, toggle works in both, past day heads stopped saying "N left" about a day that's over. Ship-check is now fully mechanical — banner===declaration across all seven files, and every CSS class app.js EMITS is scraped from the source and checked against the stylesheet rather than hand-listed (the D89/D97 hole: a hand-written list of checks is a hand-written list of holes). app 0.35.0, css 0.29.0, html 0.27.0; queue/store/celebrate/config/functions untouched. Smoke CZ–DC. HANDOFF 0.49.0. |

| 2026-07-15 | Claude Opus · **Billye** (5th instance, cont.) | **THE CLOCK GRID (D100)** — §5a's last open item on the week. Built on D93 and deliberately not on D91's diagram: task blocks END at the deadline and trail backward; only overdue runs forward. estimateMinutes shipped (store 0.11.0 — the first store change since Phase 1; addTask destructures explicitly, so it would have been silently dropped), null≠0 and unestimated blocks are dashed and say so. Drag-the-top-edge = estimate, which is D93's actual payoff. 30 geometry tests + 31 reflection tests still green. **Two process notes worth keeping.** (1) A patch script's own template literals ate backticks in the code being inserted — twice. Fix: write the new code to a PLAIN .js file and splice it in as text; never nest code inside a patch template literal. (2) The ship-check failed on its OWN stale expectations twice this session (a hardcoded ?v= literal, then `!includes('cardBtn.hidden')` which D100 legitimately falsified). Both now assert INTENT and DERIVE values from the files. Third instance of one lesson: **anything hand-written per-file in a checker is a hole; iterate and derive.** Also caught: appending checks after a `process.exit` silently skips them — a green checker that never ran. app 0.36.0, queue 0.16.0, store 0.11.0, css 0.30.0, html 0.28.0. Smoke DG–DM. HANDOFF 0.50.0. |

| 2026-07-15 | Claude Opus · **Billye** (5th instance, cont.) | **D101–D103.** Gesture lock (defer, never drop — render() is a snapshot handler; fails open after 30s), the rules pass Jake asked for in his first message (email_verified is the real hole; .lower() is insurance), and the tracks fix. **The tracks bug is the one to learn from: I wrote D100's warning that .wv-strip is a 7-track grid and a mismatched grid would misalign every strip — and then shipped exactly that in D97's Tidal compression, via a different route, and didn't connect them for three rounds.** Knowing the hazard is not the same as checking for it. Geometry declared in two places is a bug with a delay on it; it's one var now. Also corrected my own roadmap: I invented a "pick one layout" verdict that contradicted D69, and Jake caught it. Every defect this session was caught by Jake from a screenshot — banner, dead space, missing cards, misaligned heads. The checks are mechanical now and he is still the last line. | HANDOFF 0.51.0. |

## 8. Far-Future Questions (Jake's list, 2026-07-12 — for far-future instances)

Not commitments — horizons. Revisit when the 1.0 dashboard (D67) is real and Phase 3 is complete. Nothing here blocks anything current.

1. **Accessibility of calendar integration:** could the Phase-3 setup (Blaze, console, service accounts, calendar sharing) ever be packaged for less tech-savvy teachers and students? What would a "Sorting Wheel-style README" for Tentacalendar even look like when a Google Cloud project is a prerequisite? (Compare: the Sorting Ceremony README task, still pending in its own repo.)
2. **Project TYPES with preset pipelines:** "Actuarial Report," "Taiko Performance," "Unit Test" — named templates beyond the single Katie-shaped default (stage templates exist per-workspace today, D-history; this asks for a library of them, selectable at project creation). Related: project-level tiers/categories so the PROJECTS panel and year views can filter like the task queue's chips do (today, tier filters deliberately don't touch projects — D47/D65).
3. **Shared projects between users:** two people checking off stages on one pipeline. Touches rules, presence, and the celebration engine (who gets the confetti?).
4. **True multi-user / multi-interface:** Jake's projects, Katie's projects, some shared; "adoption" (Jake views Katie's board with her permission, without logging in as her); teacher teams collaborating on shared projects while keeping private personal tasks in the same queue. The workspace model (D12: single "primary") is the load-bearing wall this renovates — per-user workspaces + membership + per-doc visibility, or federated instances? Also the honest question: one deployment with real auth boundaries vs. a GitHub instance per household.

*(The nearer-term Annual overlay-calendars idea — national holidays/vacation days with tier-chip-style toggles — lives in §5c, gated behind calendar integration (D75/D80). Jake re-raised it 2026-07-12: still wanted, still post-integration.)*
